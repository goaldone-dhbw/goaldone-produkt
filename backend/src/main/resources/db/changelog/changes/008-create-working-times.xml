<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="005" author="copilot">
        <createTable tableName="working_times">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_account_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="organization_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="start_time" type="TIME">
                <constraints nullable="false"/>
            </column>
            <column name="end_time" type="TIME">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="NOW()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createTable tableName="working_time_days">
            <column name="working_time_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="day_of_week" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey
                tableName="working_time_days"
                columnNames="working_time_id, day_of_week"
                constraintName="pk_working_time_days"/>

        <addForeignKeyConstraint
                baseTableName="working_time_days"
                baseColumnNames="working_time_id"
                referencedTableName="working_times"
                referencedColumnNames="id"
                constraintName="fk_working_time_days_wt"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="working_times"
                baseColumnNames="user_account_id"
                referencedTableName="user_accounts"
                referencedColumnNames="id"
                constraintName="fk_working_times_account"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="working_times"
                baseColumnNames="organization_id"
                referencedTableName="organizations"
                referencedColumnNames="id"
                constraintName="fk_working_times_org"
                onDelete="CASCADE"/>

        <sql>
            ALTER TABLE working_times
            ADD CONSTRAINT chk_working_times_end_after_start
            CHECK (end_time &gt; start_time);
        </sql>

        <createIndex tableName="working_times" indexName="idx_working_times_account">
            <column name="user_account_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
