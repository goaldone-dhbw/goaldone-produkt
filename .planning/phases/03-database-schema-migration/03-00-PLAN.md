---
phase: 03-database-schema-migration
plan: 00
type: execute
wave: 1
depends_on: []
files_modified: [api-spec/openapi.yaml]
autonomous: true
requirements: [DB-05]

must_haves:
  truths:
    - "OpenAPI specification includes the X-Org-ID header for organization-scoped endpoints"
  artifacts:
    - path: "api-spec/openapi.yaml"
      provides: "Updated API contract with X-Org-ID header"
---

<objective>
Update the OpenAPI specification to include the X-Org-ID header for all organization-scoped endpoints, ensuring the frontend and backend agree on the multi-org context mechanism.

Purpose: Define the contract for multi-org context as required by D-05.
Output: Updated api-spec/openapi.yaml.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-database-schema-migration/03-CONTEXT.md
@api-spec/openapi.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add X-Org-ID header to OpenAPI specification</name>
  <files>api-spec/openapi.yaml</files>
  <action>
    1. Define a reusable header parameter 'OrgIdHeader' in the 'components/parameters' section:
       ```yaml
       OrgIdHeader:
         name: X-Org-ID
         in: header
         required: true
         description: Organization ID context for the request
         schema:
           type: string
           format: uuid
       ```
    2. Identify all endpoints that require organization context (e.g., tasks, appointments, member-management, schedules).
    3. Add the '$ref: "#/components/parameters/OrgIdHeader"' to the 'parameters' list for these endpoints.
    4. Ensure that the 'AccountIdPath' parameter (if still used) is consistent with the new header-based approach or document its continued use for specific account actions.
  </action>
  <verify>
    <automated>npx @redocly/cli lint api-spec/openapi.yaml</automated>
  </verify>
  <done>OpenAPI spec updated and linted. Javadoc-equivalent descriptions added to the YAML.</done>
</task>

</tasks>

<verification>
Verify that the generated API documentation (if visible) shows the X-Org-ID header for the relevant endpoints.
</verification>

<success_criteria>
- api-spec/openapi.yaml contains the X-Org-ID header definition.
- Relevant paths in the spec reference the OrgIdHeader.
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-schema-migration/03-00-SUMMARY.md`
</output>
