---
phase: 03-database-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/src/main/resources/db/changelog/changes/012-finalize-user-membership-model.xml, backend/src/main/resources/db/changelog/db.changelog-master.xml]
autonomous: true
requirements: [DB-01, DB-02, DB-03, DB-04]

must_haves:
  truths:
    - "Table 'user_identities' is renamed to 'users'"
    - "Table 'user_accounts' is renamed to 'memberships'"
    - "Column 'auth_user_id' exists in 'users' table and is unique"
    - "Column 'auth_user_id' no longer exists in 'memberships' table"
  artifacts:
    - path: "backend/src/main/resources/db/changelog/changes/012-finalize-user-membership-model.xml"
      provides: "Finalized schema migration"
  key_links:
    - from: "backend/src/main/resources/db/changelog/db.changelog-master.xml"
      to: "backend/src/main/resources/db/changelog/changes/012-finalize-user-membership-model.xml"
      via: "include file"
---

<objective>
Finalize the database schema by renaming tables to their final names (users, memberships) and normalizing the auth_user_id column.

Purpose: Align the database schema with the new "User Identity + Multiple Memberships" model.
Output: New Liquibase changelog file and updated master changelog.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-database-schema-migration/03-CONTEXT.md
@backend/src/main/resources/db/changelog/changes/010-rename-zitadel-identifiers.xml
@backend/src/main/resources/db/changelog/changes/011-allow-multiple-orgs-per-user.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create finalized schema migration changelog</name>
  <files>backend/src/main/resources/db/changelog/changes/012-finalize-user-membership-model.xml</files>
  <action>
    Create a new Liquibase changelog that:
    1. Renames table 'user_identities' to 'users' (per D-01).
    2. Renames table 'user_accounts' to 'memberships' (per D-01).
    3. Adds column 'auth_user_id' (VARCHAR(255)) to 'users' table (per D-02).
    4. Migrates data: Set 'users.auth_user_id' from 'memberships.auth_user_id' where 'memberships.user_identity_id' = 'users.id'. (Use a SQL update statement).
    5. Adds unique constraint on 'users.auth_user_id'.
    6. Removes 'auth_user_id' column from 'memberships' table.
    7. Updates foreign key constraints to reflect the renamed tables (users, memberships).
  </action>
  <verify>
    <automated>./mvnw compile liquibase:updateTestingRollback -pl backend</automated>
  </verify>
  <done>Liquibase changelog created and verified against H2.</done>
</task>

<task type="auto">
  <name>Task 2: Register changelog in master</name>
  <files>backend/src/main/resources/db/changelog/db.changelog-master.xml</files>
  <action>Add the new changelog file (012-finalize-user-membership-model.xml) to the end of db.changelog-master.xml.</action>
  <verify>
    <automated>grep "012-finalize-user-membership-model.xml" backend/src/main/resources/db/changelog/db.changelog-master.xml</automated>
  </verify>
  <done>Master changelog updated.</done>
</task>

</tasks>

<verification>
Run `./mvnw spring-boot:run -Dspring-boot.run.profiles=local` in the backend and verify that the application starts without Liquibase errors.
</verification>

<success_criteria>
- Liquibase migrations complete successfully.
- Table 'users' exists with 'auth_user_id' column.
- Table 'memberships' exists without 'auth_user_id' column.
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-schema-migration/03-01-SUMMARY.md`
</output>
