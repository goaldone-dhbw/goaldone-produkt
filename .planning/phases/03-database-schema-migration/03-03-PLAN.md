---
phase: 03-database-schema-migration
plan: 03
type: execute
wave: 3
depends_on: [03-00, 03-02]
files_modified: [backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java, backend/src/main/java/de/goaldone/backend/service/JitProvisioningService.java, backend/src/main/java/de/goaldone/backend/filter/JitProvisioningFilter.java]
autonomous: true
requirements: [DB-06]

must_haves:
  truths:
    - "CurrentUserResolver resolves MembershipEntity using X-Org-ID header"
    - "CurrentUserResolver resolves UserEntity using auth_user_id (sub) from JWT"
    - "JitProvisioningService creates UserEntity and MembershipEntity separately"
  artifacts:
    - path: "backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java"
      provides: "Header-aware user/membership resolution"
  key_links:
    - from: "backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java"
      to: "HttpServletRequest"
      via: "header extraction"
      pattern: "request\\.getHeader\\(\"X-Org-ID\"\\)"
---

<objective>
Refactor user resolution, provisioning, and the JIT filter to support the new header-based multi-org model and the split User/Membership entity structure.

Purpose: Implement the runtime logic for identifying the user's current context (User + Membership) in a multi-org environment, fulfilling D-05 and D-06.
Output: Refactored CurrentUserResolver, JitProvisioningService, and JitProvisioningFilter with full Javadoc.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-database-schema-migration/03-CONTEXT.md
@backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java
@backend/src/main/java/de/goaldone/backend/service/JitProvisioningService.java
@backend/src/main/java/de/goaldone/backend/filter/JitProvisioningFilter.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor CurrentUserResolver with Javadoc</name>
  <files>backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java</files>
  <action>
    1. Inject 'jakarta.servlet.http.HttpServletRequest' into 'CurrentUserResolver'.
    2. Add a helper method to extract the 'X-Org-ID' header and parse it as a UUID.
    3. Refactor 'resolveCurrentAccount()' (which should be renamed to 'resolveCurrentMembership()'):
       - Get 'authUserId' from JWT.
       - Get 'orgId' from 'X-Org-ID' header.
       - Use 'membershipRepository.findByUserAuthUserIdAndOrganizationId(authUserId, orgId)' to find the membership.
       - Throw 401/403 or IllegalStateException if not found (per D-06).
    4. Add 'resolveCurrentUser()' method to return the 'UserEntity'.
    5. Add comprehensive Javadoc to the class and all public methods.
  </action>
  <verify>
    <automated>./mvnw compile -pl backend</automated>
  </verify>
  <done>CurrentUserResolver refactored to use X-Org-ID and return Membership/User entities, with Javadoc.</done>
</task>

<task type="auto">
  <name>Task 2: Update JitProvisioningService with Javadoc</name>
  <files>backend/src/main/java/de/goaldone/backend/service/JitProvisioningService.java</files>
  <action>
    Update 'JitProvisioningService.provisionUser(Jwt jwt)':
    1. Extract 'auth_user_id' from JWT.
    2. Check if 'UserEntity' exists; if not, create it.
    3. Extract organization memberships from 'orgs' claim (from Phase 2 work).
    4. For each membership in JWT:
       - Find or create 'OrganizationEntity'.
       - Check if 'MembershipEntity' exists for (User, Organization).
       - Create 'MembershipEntity' if missing.
    5. Ensure 'MembershipEntity' no longer tries to store 'auth_user_id' directly.
    6. Add/update Javadoc for the class and its methods.
  </action>
  <verify>
    <automated>./mvnw compile -pl backend</automated>
  </verify>
  <done>JIT provisioning logic updated for the new schema, with Javadoc.</done>
</task>

<task type="auto">
  <name>Task 3: Update JitProvisioningFilter with Javadoc</name>
  <files>backend/src/main/java/de/goaldone/backend/filter/JitProvisioningFilter.java</files>
  <action>
    1. Update the Javadoc of 'JitProvisioningFilter' to reflect the new domain model (UserIdentityEntity -> UserEntity, UserAccountEntity -> MembershipEntity).
    2. Ensure the filter still correctly invokes 'jitProvisioningService.provisionUser(jwt)'.
    3. Verify that the filter is properly registered in the security configuration (or confirm no changes are needed there).
    4. Ensure all methods in the filter have proper Javadoc.
  </action>
  <verify>
    <automated>./mvnw compile -pl backend</automated>
  </verify>
  <done>JitProvisioningFilter updated with correct terminology and Javadoc.</done>
</task>

</tasks>

<verification>
Run `./mvnw test -pl backend` and ensure all security and provisioning integration tests pass with the new model.
</verification>

<success_criteria>
- CurrentUserResolver correctly identifies the active organization via the 'X-Org-ID' header.
- JIT provisioning correctly populates 'users' and 'memberships' tables.
- All modified classes have English Javadoc.
</success_criteria>

<output>
After completion, create `.planning/phases/03-database-schema-migration/03-03-SUMMARY.md`
</output>
