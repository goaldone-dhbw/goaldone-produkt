---
phase: 06-backend-error-fix-and-test-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/test/java/de/goaldone/backend/service/JitProvisioningServiceTest.java
  - backend/src/test/java/de/goaldone/backend/service/CurrentUserResolverTest.java
  - backend/src/test/java/de/goaldone/backend/security/SecurityIntegrationTest.java
  - backend/src/test/java/de/goaldone/backend/controller/MemberManagementControllerIT.java
  - backend/src/test/java/de/goaldone/backend/service/MemberManagementServiceTest.java
  - backend/src/test/java/de/goaldone/backend/service/MemberInviteServiceTest.java
  - backend/src/test/java/de/goaldone/backend/controller/OrganizationManagementIntegrationTest.java
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-06]

must_haves:
  truths:
    - "JIT provisioning logic works with new UserEntity and MembershipEntity models"
    - "JWT validation correctly extracts roles and user identity for auth-service tokens"
    - "Member management operations (invite, reinvite, role change, remove) are tested"
    - "Organization management CRUD and multi-org authorization are verified"
    - "SecurityIntegrationTest verifies authorities claim parsing and role-based access control"
  artifacts:
    - path: "backend/src/test/java/de/goaldone/backend/service/JitProvisioningServiceTest.java"
      provides: "JIT provisioning tests adapted to new entity model"
      contains: "provisionUser"
    - path: "backend/src/test/java/de/goaldone/backend/service/CurrentUserResolverTest.java"
      provides: "User/org context resolver tests"
      contains: "resolveCurrentAccount"
    - path: "backend/src/test/java/de/goaldone/backend/security/SecurityIntegrationTest.java"
      provides: "JWT validation and role extraction tests"
      contains: "authorities"
    - path: "backend/src/test/java/de/goaldone/backend/controller/MemberManagementControllerIT.java"
      provides: "Member management API integration tests"
      contains: "@SpringBootTest"
  key_links:
    - from: "JitProvisioningServiceTest"
      to: "UserEntity, MembershipEntity"
      via: "test setup fixtures"
      pattern: "new UserEntity|new MembershipEntity"
    - from: "SecurityIntegrationTest"
      to: "JwtAuthenticationConverter"
      via: "role extraction verification"
      pattern: "authorities|ROLE_"
    - from: "MemberManagementControllerIT"
      to: "AuthServiceManagementClient"
      via: "MockMvc + WireMock mocking"
      pattern: "stubFor|MockMvc"
---

<objective>
Restore 7 high-priority test files covering JWT validation, JIT provisioning, and member management. These tests are foundational to the auth-service migration and are adapted to the new UserEntity/MembershipEntity model.

Purpose: Verify that the auth-service token contract is correctly handled by the backend; establish baseline test coverage for the cutover's core functionality.
Output: 7 test files with 35-45 passing tests (measured from git history), all adapted to new entity model and auth-service JWT claim shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-backend-error-fix-and-test-restoration/06-CONTEXT.md
@memory/IT JWT Auth Pattern.md

Deleted test references (from commit 364b2af^):
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/JitProvisioningServiceTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/CurrentUserResolverTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/security/SecurityIntegrationTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/controller/MemberManagementControllerIT.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/MemberManagementServiceTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/MemberInviteServiceTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/controller/OrganizationManagementIntegrationTest.java
</context>

<tasks>

<task type="checkpoint">
  <name>Task 0: Pre-flight verification — Confirm backend compiles and starts</name>
  <action>
Per D-01 (Fix Errors First), verify backend is in good state before test restoration:

```bash
cd backend
./mvnw clean compile
./mvnw spring-boot:run -Dspring-boot.run.profiles=local &
sleep 5
curl http://localhost:8080/api/v1/health
kill %1  # stop the running process
```

Expected results:
- BUILD SUCCESS with zero compilation errors
- Backend starts on port 8080 within 3 seconds
- Health endpoint returns 200
- No startup exceptions in logs

If any errors appear, investigate and resolve BEFORE proceeding to test restoration.
  </action>
  <verify>
    <manual>Backend compiles, starts cleanly, and health check passes</manual>
  </verify>
  <done>
    Pre-flight checks complete — backend is ready for test restoration.
  </done>
</task>

<task type="auto">
  <name>Task 1: Resurrect and adapt JWT and JIT provisioning tests</name>
  <files>
    backend/src/test/java/de/goaldone/backend/service/JitProvisioningServiceTest.java,
    backend/src/test/java/de/goaldone/backend/service/CurrentUserResolverTest.java,
    backend/src/test/java/de/goaldone/backend/security/SecurityIntegrationTest.java
  </files>
  <read_first>
    backend/src/main/java/de/goaldone/backend/entity/UserEntity.java,
    backend/src/main/java/de/goaldone/backend/entity/MembershipEntity.java,
    backend/src/main/java/de/goaldone/backend/service/JitProvisioningService.java,
    backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java,
    backend/src/main/java/de/goaldone/backend/config/SecurityConfig.java,
    backend/src/test/java/de/goaldone/backend/filter/JitProvisioningFilterTest.java
  </read_first>
  <action>
Per D-03 (hybrid resurrection), retrieve deleted tests from git history using:
```bash
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/JitProvisioningServiceTest.java > /tmp/JitProvisioningServiceTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/CurrentUserResolverTest.java > /tmp/CurrentUserResolverTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/security/SecurityIntegrationTest.java > /tmp/SecurityIntegrationTest.java
```

**JitProvisioningServiceTest.java** — Resurrect and adapt per D-06 (test happy path + error cases):
- Replace old `UserAccountEntity`/`UserIdentityEntity` references with `UserEntity` and `MembershipEntity`
- Update test fixtures to create `UserEntity` with auth_user_id instead of old mapping fields
- Update test fixtures to create `MembershipEntity` with organization reference and role
- Verify `provisionUser()` creates or updates `MembershipEntity` correctly
- Add test case for multi-org provisioning (user already exists in one org, added to another)
- Add test case for invitation acceptance flow (invited user first login)
- Add test case for race condition handling (two concurrent provision attempts)
- Verify role extraction from JWT `authorities` claim (not old Zitadel URN format)

**CurrentUserResolverTest.java** — Resurrect and adapt per D-06:
- Replace `UserAccountEntity`/`UserIdentityEntity` with `UserEntity`/`MembershipEntity`
- Verify `resolveCurrentAccount()` returns `MembershipEntity` for authenticated user in requested org
- Test error case: user is authenticated but not member of requested org
- Verify `extractJwt()` correctly gets JWT from SecurityContext
- Test multi-org scenario: user has memberships in multiple orgs, resolver picks the right one from X-Org-ID header

**SecurityIntegrationTest.java** — Resurrect and adapt per D-07 (MockMvc JWT helpers):
- Use `jwt().authorities("ROLE_COMPANY_ADMIN", "ROLE_COMPANY_MEMBER")` pattern (explicit authorities required)
- Test that valid JWT with correct authorities passes Spring Security filter chain
- Test that missing authorities results in 403 Forbidden on protected endpoints
- Test JWT signature validation failure results in 401 Unauthorized
- Test role extraction from `authorities` claim (not old Zitadel claim structure)
- Test multi-org RBAC: user with COMPANY_ADMIN in org A can access org A but not org B (403)

Follow existing test style from `JitProvisioningFilterTest.java` and `SharedWiremockSetup.java` patterns.
Use assertions consistent with current codebase (AssertJ or standard JUnit).
  </action>
  <verify>
    <automated>cd backend && ./mvnw test -Dtest=JitProvisioningServiceTest,CurrentUserResolverTest,SecurityIntegrationTest -DfailIfNoTests=false</automated>
  </verify>
  <done>
    All three test files created with total 11-13 tests, all passing.
    Breakdown: JitProvisioningServiceTest (3), CurrentUserResolverTest (5), SecurityIntegrationTest (3-5).
    JIT provisioning logic verified with new UserEntity/MembershipEntity model.
    JWT authorities claim extraction verified against auth-service token format.
    Multi-org scenarios tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resurrect and adapt member management service and controller tests</name>
  <files>
    backend/src/test/java/de/goaldone/backend/controller/MemberManagementControllerIT.java,
    backend/src/test/java/de/goaldone/backend/service/MemberManagementServiceTest.java,
    backend/src/test/java/de/goaldone/backend/service/MemberInviteServiceTest.java
  </files>
  <read_first>
    backend/src/main/java/de/goaldone/backend/controller/MemberManagementController.java,
    backend/src/main/java/de/goaldone/backend/service/MemberManagementService.java,
    backend/src/main/java/de/goaldone/backend/service/MemberInviteService.java,
    backend/src/main/java/de/goaldone/backend/client/AuthServiceManagementClient.java,
    backend/src/test/java/de/goaldone/backend/SharedWiremockSetup.java
  </read_first>
  <action>
Retrieve deleted tests from git history:
```bash
git show 364b2af^:backend/src/test/java/de/goaldone/backend/controller/MemberManagementControllerIT.java > /tmp/MemberManagementControllerIT.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/MemberManagementServiceTest.java > /tmp/MemberManagementServiceTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/MemberInviteServiceTest.java > /tmp/MemberInviteServiceTest.java
```

**MemberManagementControllerIT.java** — Resurrect and adapt per D-08 (integration tests with @SpringBootTest):
- Use `@SpringBootTest` + `MockMvc` for HTTP simulation
- Mock auth-service endpoints with `@ExtendWith(WireMockExtension.class)` using `SharedWiremockSetup` pattern
- Test inviteMember(org, email, role) returns 201, calls auth-service invite endpoint
- Test reinviteMember(org, userId) returns 204
- Test listMembers(org) returns 200 with member list (mock auth-service response)
- Test changeMemberRole(org, userId, newRole) returns 200 (mock auth-service update)
- Test removeMember(org, userId) returns 204 (mock auth-service delete)
- Test error case: auth-service returns 409 (last admin conflict) → propagate to client as 409
- Test error case: auth-service returns 5xx → propagate as 502
- Test authorization: non-admin user gets 403
- Test authorization: user not member of org gets 403
- Use explicit `.authorities("ROLE_COMPANY_ADMIN")` in JWT helpers per D-07 and memory note

**MemberManagementServiceTest.java** — Resurrect and adapt per D-06:
- Replace `UserAccountEntity`/`UserIdentityEntity` with `UserEntity`/`MembershipEntity`
- Test `listMembers()` fetches from auth-service and builds response
- Test `changeMemberRole()` calls auth-service and updates local membership role
- Test `removeMember()` calls auth-service and verifies last-admin guard
- Test error case: removing last admin throws or propagates auth-service error

**MemberInviteServiceTest.java** — Resurrect and adapt per D-06:
- Test `inviteMember()` creates INVITED membership locally and calls auth-service invite endpoint
- Test `reinviteMember()` updates invitation_id and calls auth-service reinvite
- Test invitation acceptance flow (user with invitation_id first login → membership status changes from INVITED to ACTIVE)
- Test error case: inviting user already in org

All three tests must use `SharedWiremockSetup` pattern for auth-service mocking per D-05.
All controller tests must use explicit `.authorities(...)` per D-07.
  </action>
  <verify>
    <automated>cd backend && ./mvnw test -Dtest=MemberManagementControllerIT,MemberManagementServiceTest,MemberInviteServiceTest -DfailIfNoTests=false</automated>
  </verify>
  <done>
    All three test files created with total ~20-25 tests, all passing.
    Member management API endpoints verified via MockMvc + WireMock.
    Auth-service M2M calls mocked and verified.
    Invitation flow tested.
  </done>
</task>

<task type="auto">
  <name>Task 3: Resurrect organization management integration tests</name>
  <files>
    backend/src/test/java/de/goaldone/backend/controller/OrganizationManagementIntegrationTest.java
  </files>
  <read_first>
    backend/src/main/java/de/goaldone/backend/controller/OrgManagementController.java,
    backend/src/main/java/de/goaldone/backend/service/OrganizationManagementService.java,
    backend/src/test/java/de/goaldone/backend/SharedWiremockSetup.java
  </read_first>
  <action>
Retrieve deleted test from git history:
```bash
git show 364b2af^:backend/src/test/java/de/goaldone/backend/controller/OrganizationManagementIntegrationTest.java > /tmp/OrganizationManagementIntegrationTest.java
```

**OrganizationManagementIntegrationTest.java** — Resurrect and adapt per D-08:
- Use `@SpringBootTest` + `MockMvc` for HTTP simulation
- Mock auth-service endpoints with `@ExtendWith(WireMockExtension.class)` per D-05
- Test createOrganization(name, description) returns 201, creates org with admin invitation
- Test createOrganization with auth-service 4xx error (e.g., name already exists) → propagate as 409
- Test createOrganization with auth-service 5xx → propagate as 502
- Test per-org authorization: users can only access orgs they are members of
- Test multi-org scenario: user with memberships in two orgs can access both independently
- Test X-Org-ID header handling: requests must include org context
- All tests use explicit `.authorities(...)` per D-07

Replace `UserAccountEntity`/`UserIdentityEntity` with `UserEntity`/`MembershipEntity`.
Verify organization creation flow calls auth-service correctly.

**Entity Model Adaptation Guide for all tests in this task:**
- Old `UserAccountEntity` → New `UserEntity` (fields: id, authUserId, emails, lastSeenAt)
- Old `UserIdentityEntity` → Removed; logic integrated into `UserEntity`
- Old `accountId` parameter → New `userId` (UUID from auth-service)
- Fixture changes:
  ```java
  // Old: UserAccountEntity account = new UserAccountEntity();
  // New:
  UserEntity user = new UserEntity();
  user.setId(UUID.randomUUID());  // Use auth-service user ID
  user.setAuthUserId("auth-service-uuid");
  user.setEmails(List.of("test@example.com"));
  userRepository.save(user);
  
  // Old: UserIdentityEntity identity = new UserIdentityEntity();
  // (No longer needed; user creation handles identity)
  ```
- Test assertions: Replace `userAccountRepository.findById(accountId)` with `userRepository.findById(userId)`
  </action>
  <verify>
    <automated>cd backend && ./mvnw test -Dtest=OrganizationManagementIntegrationTest -DfailIfNoTests=false</automated>
  </verify>
  <done>
    OrganizationManagementIntegrationTest created with 6-8 tests, all passing.
    Organization CRUD and multi-org authorization verified.
    Auth-service integration tested.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `cd backend && ./mvnw test` — verify all tests pass (target: 35-45 tests total for this plan, measured from git history)
2. Verify no test failures; if any failures, identify if test adaptation is incomplete or code issue
3. Confirm pre-flight checkpoint passed (backend compiles and starts cleanly)
4. Confirm JIT provisioning logic works with new entity model (check test logs for success messages)
5. Confirm member management operations are tested with auth-service mocking
6. Confirm SecurityIntegrationTest verifies JWT claim parsing and role-based access control with auth-service token format
7. Count actual tests: `./mvnw test -Dtest=JitProvisioningServiceTest,CurrentUserResolverTest,SecurityIntegrationTest,MemberManagementControllerIT,MemberManagementServiceTest,MemberInviteServiceTest,OrganizationManagementIntegrationTest 2>&1 | grep "Tests run:"`
</verification>

<success_criteria>
- Pre-flight verification complete: backend compiles with zero errors and starts cleanly
- All 7 test files created and committed
- Total of 35-45 passing tests across these files (measured from git history)
  - JitProvisioningServiceTest: 3 tests
  - CurrentUserResolverTest: 5 tests
  - SecurityIntegrationTest: 3-5 tests
  - MemberManagementControllerIT: 8-10 tests
  - MemberManagementServiceTest: 4-6 tests
  - MemberInviteServiceTest: 4-6 tests
  - OrganizationManagementIntegrationTest: 6-8 tests
- JWT validation and role extraction working correctly with auth-service token format
- JIT provisioning logic verified with new UserEntity/MembershipEntity model
- Member management operations tested with auth-service mocking and error cases
- Organization management tested with multi-org authorization checks
- All explicit `.authorities(...)` patterns in place per D-07
- Zero test failures
- Ready to proceed to Wave 2
</success_criteria>

<output>
After completion, create `.planning/phases/06-backend-error-fix-and-test-restoration/06-01-SUMMARY.md`
</output>
