---
phase: 06-backend-error-fix-and-test-restoration
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - backend/src/test/java/de/goaldone/backend/controller/SuperAdminIntegrationTest.java
  - backend/src/test/java/de/goaldone/backend/controller/AccountLinkingIntegrationTest.java
  - backend/src/test/java/de/goaldone/backend/service/AccountLinkingServiceTest.java
  - backend/src/test/java/de/goaldone/backend/service/SuperAdminServiceTest.java
  - backend/src/test/java/de/goaldone/backend/service/UserIdentityServiceTest.java
  - backend/src/test/java/de/goaldone/backend/service/TasksServiceTest.java
  - backend/src/test/java/de/goaldone/backend/service/WorkingTimesServiceTest.java
  - backend/src/test/java/de/goaldone/backend/service/UserAccountDeletionServiceIntegrationTest.java
  - backend/src/test/java/de/goaldone/backend/integration/MemberManagementAuthServiceIntegrationTest.java
  - backend/src/test/java/de/goaldone/backend/integration/AuthServiceM2MClientTest.java
  - backend/src/test/java/de/goaldone/backend/integration/RoleExtractionAuthServiceTest.java
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06]

must_haves:
  truths:
    - "Super admin management operations (invite, list, remove) are tested"
    - "Account linking (request, confirm, unlink) operations are tested"
    - "Non-critical services (tasks, working times, user identity) have test coverage"
    - "Auth-service M2M client credentials flow is tested (token fetch, caching, refresh)"
    - "Auth-service member operations (invite/role/remove) integration tested with error cases"
    - "Role extraction from auth-service JWT authorities claim is verified"
  artifacts:
    - path: "backend/src/test/java/de/goaldone/backend/controller/SuperAdminIntegrationTest.java"
      provides: "Super admin API integration tests"
      contains: "@SpringBootTest"
    - path: "backend/src/test/java/de/goaldone/backend/controller/AccountLinkingIntegrationTest.java"
      provides: "Account linking API integration tests"
      contains: "MockMvc"
    - path: "backend/src/test/java/de/goaldone/backend/integration/AuthServiceM2MClientTest.java"
      provides: "M2M client credentials flow tests"
      contains: "ClientCredentials"
    - path: "backend/src/test/java/de/goaldone/backend/integration/MemberManagementAuthServiceIntegrationTest.java"
      provides: "Auth-service member operations integration tests"
      contains: "stubFor"
  key_links:
    - from: "AuthServiceM2MClientTest"
      to: "AuthServiceManagementClient"
      via: "M2M token authentication"
      pattern: "credentials|token|refresh"
    - from: "MemberManagementAuthServiceIntegrationTest"
      to: "AuthServiceManagementClient"
      via: "member operation calls"
      pattern: "invite|role|remove"
    - from: "RoleExtractionAuthServiceTest"
      to: "JwtAuthenticationConverter"
      via: "authority conversion"
      pattern: "authorities"
---

<objective>
Restore 8 secondary test files and write 3 comprehensive auth-service integration tests. These cover admin features, account linking, task/working time services, and new auth-service integration points (M2M credentials, member operations, role extraction).

Purpose: Complete test restoration for all Phase 5 functionality; verify auth-service integration is robust and error cases are handled.
Output: 11 test files with 48-68 passing tests (measured from git history + new tests), covering all secondary features and auth-service integration scenarios.
Combined Wave 1 + Wave 2 total: 83-113 tests (target: 100+).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-backend-error-fix-and-test-restoration/06-CONTEXT.md
@memory/IT JWT Auth Pattern.md

Deleted test references (from commit 364b2af^):
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/controller/SuperAdminIntegrationTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/controller/AccountLinkingIntegrationTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/AccountLinkingServiceTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/SuperAdminServiceTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/UserIdentityServiceTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/TasksServiceTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/WorkingTimesServiceTest.java
- git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/UserAccountDeletionServiceIntegrationTest.java

**Scope Notes:**
- Phase 6 focuses on backend test restoration and error fixes
- Frontend E2E tests (Angular integration, auth flow UI) are out of scope — Phase 4 covered frontend auth switch
- Infrastructure regression testing (Docker, auth-service deployment) is out of scope — deployment validation happens at CI/CD level
- Performance/load testing is deferred to post-v1.0
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resurrect secondary tests (SuperAdmin, AccountLinking, UserIdentity)</name>
  <files>
    backend/src/test/java/de/goaldone/backend/controller/SuperAdminIntegrationTest.java,
    backend/src/test/java/de/goaldone/backend/controller/AccountLinkingIntegrationTest.java,
    backend/src/test/java/de/goaldone/backend/service/SuperAdminServiceTest.java,
    backend/src/test/java/de/goaldone/backend/service/AccountLinkingServiceTest.java,
    backend/src/test/java/de/goaldone/backend/service/UserIdentityServiceTest.java
  </files>
  <read_first>
    backend/src/main/java/de/goaldone/backend/entity/UserEntity.java,
    backend/src/main/java/de/goaldone/backend/entity/MembershipEntity.java,
    backend/src/main/java/de/goaldone/backend/service/SuperAdminService.java,
    backend/src/main/java/de/goaldone/backend/service/AccountLinkingService.java,
    backend/src/main/java/de/goaldone/backend/service/UserIdentityService.java,
    backend/src/test/java/de/goaldone/backend/SharedWiremockSetup.java
  </read_first>
  <action>
Retrieve deleted tests from git history:
```bash
git show 364b2af^:backend/src/test/java/de/goaldone/backend/controller/SuperAdminIntegrationTest.java > /tmp/SuperAdminIntegrationTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/controller/AccountLinkingIntegrationTest.java > /tmp/AccountLinkingIntegrationTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/SuperAdminServiceTest.java > /tmp/SuperAdminServiceTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/AccountLinkingServiceTest.java > /tmp/AccountLinkingServiceTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/UserIdentityServiceTest.java > /tmp/UserIdentityServiceTest.java
```

**SuperAdminIntegrationTest.java** — Resurrect and adapt per D-08:
- Use `@SpringBootTest` + `MockMvc`
- Mock auth-service endpoints with `@ExtendWith(WireMockExtension.class)` per D-05
- Test inviteSuperAdmin(email, role) returns 201
- Test listSuperAdmins() returns 200 with list
- Test deleteSuperAdmin(userId) returns 204
- Test error case: auth-service returns 409 (conflict) → propagate
- Test authorization: only SUPER_ADMIN users can invoke these endpoints
- Use explicit `.authorities("ROLE_SUPER_ADMIN")` per D-07

**AccountLinkingIntegrationTest.java** — Resurrect and adapt per D-08:
- Use `@SpringBootTest` + `MockMvc`
- Test requestAccountLink(accountId) returns 200 with link token
- Test confirmAccountLink(linkToken, otherAccountId) returns 200
- Test unlinkAccount(accountId) returns 204
- Test getMyAccounts() returns 200 with account list
- Test error case: invalid or expired link token → 410 GONE
- Replace old entity references with `UserEntity`/`MembershipEntity`
- Test authorization: users can only link/unlink their own accounts

**SuperAdminServiceTest.java** — Resurrect and adapt per D-06:
- Replace `UserAccountEntity`/`UserIdentityEntity` with `UserEntity`/`MembershipEntity`
- Test `inviteSuperAdmin()` calls auth-service and creates record
- Test `listSuperAdmins()` fetches list from auth-service
- Test `deleteSuperAdmin()` calls auth-service delete

**AccountLinkingServiceTest.java** — Resurrect and adapt per D-06:
- Test account linking logic with new entity model
- Test request and confirm flows

**UserIdentityServiceTest.java** — Resurrect and adapt per D-06:
- Test UserIdentity (now UserEntity) operations
- Test user lookup and creation

All tests must use `SharedWiremockSetup` pattern for auth-service mocking.
Replace all old entity references with new model.
  </action>
  <verify>
    <automated>cd backend && ./mvnw test -Dtest=SuperAdminIntegrationTest,AccountLinkingIntegrationTest,SuperAdminServiceTest,AccountLinkingServiceTest,UserIdentityServiceTest -DfailIfNoTests=false</automated>
  </verify>
  <done>
    All 5 test files created with total 17-22 tests, all passing.
    Breakdown: SuperAdminIntegrationTest (5), AccountLinkingIntegrationTest (5), SuperAdminServiceTest (3), AccountLinkingServiceTest (3), UserIdentityServiceTest (3).
    Super admin operations verified.
    Account linking flow tested.
    User identity operations verified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resurrect service tests for tasks and working times</name>
  <files>
    backend/src/test/java/de/goaldone/backend/service/TasksServiceTest.java,
    backend/src/test/java/de/goaldone/backend/service/WorkingTimesServiceTest.java,
    backend/src/test/java/de/goaldone/backend/service/UserAccountDeletionServiceIntegrationTest.java
  </files>
  <read_first>
    backend/src/main/java/de/goaldone/backend/service/TasksService.java,
    backend/src/main/java/de/goaldone/backend/service/WorkingTimesService.java,
    backend/src/main/java/de/goaldone/backend/service/UserAccountDeletionService.java
  </read_first>
  <action>
Retrieve deleted tests from git history:
```bash
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/TasksServiceTest.java > /tmp/TasksServiceTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/WorkingTimesServiceTest.java > /tmp/WorkingTimesServiceTest.java
git show 364b2af^:backend/src/test/java/de/goaldone/backend/service/UserAccountDeletionServiceIntegrationTest.java > /tmp/UserAccountDeletionServiceIntegrationTest.java
```

**TasksServiceTest.java** — Resurrect and adapt per D-06:
- Replace old entity references with new model
- Test task CRUD operations

**WorkingTimesServiceTest.java** — Resurrect and adapt per D-06:
- Replace old entity references with new model
- Test working time CRUD operations
- Test validation logic

**UserAccountDeletionServiceIntegrationTest.java** — Resurrect and adapt per D-08:
- Use `@SpringBootTest` if needed
- Test account deletion cascade logic
- Verify related data is cleaned up

These are non-critical for Phase 6 focus but important for comprehensive coverage.
  </action>
  <verify>
    <automated>cd backend && ./mvnw test -Dtest=TasksServiceTest,WorkingTimesServiceTest,UserAccountDeletionServiceIntegrationTest -DfailIfNoTests=false</automated>
  </verify>
  <done>
    All 3 test files created with total 13-18 tests, all passing.
    Breakdown: TasksServiceTest (5-8), WorkingTimesServiceTest (5-8), UserAccountDeletionServiceIntegrationTest (3-5).
    Task and working time operations verified.
    User account deletion cascade tested.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write new auth-service integration tests (M2M credentials, member ops, role extraction)</name>
  <files>
    backend/src/test/java/de/goaldone/backend/integration/AuthServiceM2MClientTest.java,
    backend/src/test/java/de/goaldone/backend/integration/MemberManagementAuthServiceIntegrationTest.java,
    backend/src/test/java/de/goaldone/backend/integration/RoleExtractionAuthServiceTest.java
  </files>
  <read_first>
    backend/src/main/java/de/goaldone/backend/client/AuthServiceManagementClient.java,
    backend/src/main/java/de/goaldone/backend/service/MemberInviteService.java,
    backend/src/main/java/de/goaldone/backend/service/MemberManagementService.java,
    backend/src/main/java/de/goaldone/backend/config/SecurityConfig.java,
    backend/src/test/java/de/goaldone/backend/SharedWiremockSetup.java
  </read_first>
  <action>
Write three comprehensive auth-service integration test files per D-05 (WireMock for mocking), D-06 (happy path + error cases):

**AuthServiceM2MClientTest.java** — Test M2M client credentials flow:
- Use `@SpringBootTest` + `@ExtendWith(WireMockExtension.class)` for auth-service mocking
- Mock auth-service `/oauth2/token` endpoint to return valid M2M token with 3600s TTL
- Test `AuthServiceManagementClient` calls auth-service with correct client credentials
- Test token caching: subsequent calls within TTL use cached token, no new requests
- Test token refresh: after TTL expires, new token is fetched
- Test error case: auth-service returns 401 (invalid credentials) → exception propagated
- Test error case: auth-service returns 5xx → exception propagated
- Test error case: network timeout → exception handled gracefully
- Verify JWT payload contains correct claims (sub, aud, scope, etc.)

**MemberManagementAuthServiceIntegrationTest.java** — Test member operations via auth-service:
- Mock auth-service endpoints for member operations:
  - POST `/members/invite` — returns 200 with invitation ID
  - PATCH `/members/{id}/role` — returns 200
  - DELETE `/members/{id}` — returns 200 or 409 (last admin) per D-06
  - GET `/members?org_id=...` — returns 200 with member list
- Test invite flow: service calls auth-service, gets invitationId, stores locally
- Test role change flow: service calls auth-service, updates membership role locally
- Test removal flow: service calls auth-service, deletes membership locally
- Test error case: auth-service returns 409 (last admin) → LastAdminException propagated per D-06
- Test error case: auth-service returns 400 (invalid input) → validation error propagated
- Test error case: auth-service returns 5xx → service error propagated
- Test error case: race condition (concurrent remove attempts) — second attempt sees 404
- Verify X-Org-ID header is correctly passed to auth-service per D-09

**RoleExtractionAuthServiceTest.java** — Test role extraction from new auth-service JWT:
- Create mock JWT tokens with `authorities` claim containing roles (e.g., `["ROLE_COMPANY_ADMIN", "ROLE_COMPANY_MEMBER"]`)
- Use `JwtAuthenticationConverter` to extract authorities from JWT
- Verify `authorities` claim is correctly parsed to Spring Security authorities (not old Zitadel URN format)
- Test edge case: missing `authorities` claim → empty authorities list
- Test edge case: authorities claim is empty array → no authorities
- Test edge case: authorities claim contains invalid role format → gracefully handle or skip
- Verify role-based access control works: user with COMPANY_ADMIN authority passes `@PreAuthorize("hasRole('COMPANY_ADMIN')")`
- Verify user without role fails: 403 Forbidden

All three tests use `SharedWiremockSetup` pattern for HTTP mocking per D-05.
Use WireMock `stubFor()` to mock auth-service responses.
Verify both success and error scenarios per D-06.
Commit messages: `test(06-02): add M2M credentials flow tests`, `test(06-02): add member operations auth-service integration tests`, `test(06-02): add role extraction from auth-service JWT tests`

**New Test File Estimates (write fresh per D-04):**
- AuthServiceM2MClientTest: 8-10 tests (token fetch, caching, refresh, errors)
- MemberManagementAuthServiceIntegrationTest: 8-12 tests (invite, role, remove, error cases)
- RoleExtractionAuthServiceTest: 4-6 tests (authorities parsing, edge cases, RBAC)
Total new tests: 20-28 tests
  </action>
  <verify>
    <automated>cd backend && ./mvnw test -Dtest=AuthServiceM2MClientTest,MemberManagementAuthServiceIntegrationTest,RoleExtractionAuthServiceTest -DfailIfNoTests=false</automated>
  </verify>
  <done>
    All 3 new test files created with total 20-28 tests, all passing.
    M2M client credentials flow verified with token caching and refresh.
    Member operations (invite, role, remove) tested with auth-service mocking and error cases.
    Role extraction from auth-service JWT verified.
    All error scenarios tested per D-06.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `cd backend && ./mvnw test` — verify all tests pass (target: 48-68 tests for this plan)
2. Verify no test failures; if any failures, identify if test adaptation is incomplete or code issue
3. Confirm M2M client credentials flow is tested with token caching and refresh
4. Confirm member operations (invite, role, remove) are tested with error cases (409, 5xx, timeouts)
5. Confirm role extraction from auth-service JWT is correct
6. Count actual tests: `./mvnw test -Dtest=SuperAdminIntegrationTest,AccountLinkingIntegrationTest,SuperAdminServiceTest,AccountLinkingServiceTest,UserIdentityServiceTest,TasksServiceTest,WorkingTimesServiceTest,UserAccountDeletionServiceIntegrationTest,AuthServiceM2MClientTest,MemberManagementAuthServiceIntegrationTest,RoleExtractionAuthServiceTest 2>&1 | grep "Tests run:"`
7. Verify total test count reaches 100+ (combined 06-01 + 06-02)
</verification>

<success_criteria>
- All 11 test files created and committed (8 resurrected + 3 new)
- Total of 48-68 passing tests across this plan
  - Secondary tests (Task 1): 17-22 tests
  - Service tests (Task 2): 13-18 tests
  - New auth-service integration tests (Task 3): 20-28 tests
- Combined total of 83-113 tests across both Phase 6 plans (target: 100+ ✓)
- M2M client credentials flow tested with token caching and refresh lifecycle
- Member operations tested with auth-service mocking and error cases (409, 5xx, timeouts)
- Role extraction from auth-service JWT verified with edge cases
- Secondary features (tasks, working times, account linking, super admins) comprehensively tested
- All explicit `.authorities(...)` patterns in place per D-07
- Entity model adaptation complete (all old UserAccountEntity/UserIdentityEntity references replaced)
- Zero test failures
- Backend starts cleanly and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-backend-error-fix-and-test-restoration/06-02-SUMMARY.md`
</output>
