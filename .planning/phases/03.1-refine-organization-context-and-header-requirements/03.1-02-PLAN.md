---
phase: 03.1-refine-organization-context-and-header-requirements
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - backend/src/main/java/de/goaldone/backend/security/CurrentUserResolver.java
  - backend/src/main/java/de/goaldone/backend/controller/MembershipController.java
  - backend/src/main/java/de/goaldone/backend/controller/TaskController.java
  - backend/src/main/java/de/goaldone/backend/service/MembershipService.java
  - backend/src/main/java/de/goaldone/backend/service/TaskService.java
  - backend/src/test/java/de/goaldone/backend/security/SecurityIntegrationTest.java
autonomous: true
requirements: [AUTHZ-01, AUTHZ-02]
must_haves:
  truths:
    - "Endpoints function correctly without path-based account/org identifiers."
    - "Data is automatically filtered by the organization ID provided in the header."
    - "Multi-org integration tests pass, confirming isolation and role enforcement."
  artifacts:
    - path: "backend/src/main/java/de/goaldone/backend/security/CurrentUserResolver.java"
      provides: "Header-aware user/membership resolution"
    - path: "backend/src/test/java/de/goaldone/backend/security/SecurityIntegrationTest.java"
      provides: "Automated verification of multi-tenant security"
  key_links:
    - from: "Controllers"
      to: "TenantContext"
      via: "Indirectly via CurrentUserResolver or direct access"
---

<objective>
Refactor backend controllers and services to utilize header-based organization context. This involves removing legacy path parameters and updating data resolution logic to favor the X-Org-ID header and JWT memberships.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-refine-organization-context-and-header-requirements/3.1-CONTEXT.md
@.planning/phases/03.1-refine-organization-context-and-header-requirements/03.1-RESEARCH.md
@.planning/phases/03.1-refine-organization-context-and-header-requirements/03.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CurrentUserResolver</name>
  <files>backend/src/main/java/de/goaldone/backend/security/CurrentUserResolver.java</files>
  <action>
    - Modify `CurrentUserResolver` to prioritize `TenantContext.getOrgId()` (D-01).
    - If `X-Org-ID` is present: Resolve the specific membership for the user in that organization.
    - If `X-Org-ID` is missing (allowed for GET lists): Support returning a list of all memberships or a 'global' user context (D-03).
    - Remove dependencies on path-based `{accountId}` or `{orgId}`.
  </action>
  <verify>
    <automated>./mvnw clean compile</automated>
  </verify>
  <done>User resolution is header-aware and path-independent.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Controllers and Services</name>
  <files>
    backend/src/main/java/de/goaldone/backend/controller/MembershipController.java
    backend/src/main/java/de/goaldone/backend/controller/TaskController.java
    backend/src/main/java/de/goaldone/backend/service/MembershipService.java
    backend/src/main/java/de/goaldone/backend/service/TaskService.java
  </files>
  <action>
    - Update all endpoint signatures in `MembershipController` and `TaskController` to remove `@PathVariable UUID accountId` and `@PathVariable UUID orgId` (D-04, D-05).
    - Update member management endpoint to use the new unified path `/organization/members` (D-06).
    - Update Service methods to fetch the active organization from `TenantContext` or `CurrentUserResolver` instead of taking it as a parameter.
    - Ensure Repository queries are updated to filter by the resolved org ID.
  </action>
  <verify>
    <automated>./mvnw clean compile</automated>
  </verify>
  <done>Controllers and services are refactored to use header-based context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Security Integration Tests</name>
  <files>backend/src/test/java/de/goaldone/backend/security/SecurityIntegrationTest.java</files>
  <behavior>
    - Test: GET /tasks with X-Org-ID=A returns only Org A tasks.
    - Test: GET /tasks with no header returns tasks from all user's organizations (A and B).
    - Test: POST /tasks with X-Org-ID=A succeeds.
    - Test: POST /tasks with no header returns 400 or 403 (Mandatory for actions).
    - Test: POST /tasks with X-Org-ID=C (user is not member) returns 403.
    - Test: Endpoint with @PreAuthorize("hasOrgRole('ADMIN')") returns 403 if user is only MEMBER in Org A.
  </behavior>
  <action>
    - Create/Update integration tests using `MockMvc`.
    - Use `@WithMockUser` or custom JWT builders to simulate different memberships and roles.
    - Verify header-based isolation and multi-org logic.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=SecurityIntegrationTest</automated>
  </verify>
  <done>Security integration tests confirm robust multi-tenant enforcement.</done>
</task>

</tasks>

<success_criteria>
- Backend controllers and services no longer use path-based organization identifiers.
- Security integration tests pass for multi-org scenarios.
- Data isolation is correctly enforced via the X-Org-ID header.
</success_criteria>
