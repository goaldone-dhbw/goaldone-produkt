---
phase: 03.1-refine-organization-context-and-header-requirements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api-spec/openapi.yaml
  - backend/src/main/java/de/goaldone/backend/security/TenantContext.java
  - backend/src/main/java/de/goaldone/backend/security/TenantContextFilter.java
  - backend/src/main/java/de/goaldone/backend/security/expression/CustomMethodSecurityExpressionRoot.java
  - backend/src/main/java/de/goaldone/backend/config/MethodSecurityConfig.java
autonomous: true
requirements: [AUTHZ-01, AUTHZ-02, AUTHZ-03]
must_haves:
  truths:
    - "OpenAPI spec defines X-Org-ID header as the source of truth for organization context."
    - "Backend successfully extracts and validates the X-Org-ID header against JWT memberships."
    - "Security expressions are available to verify per-org roles."
  artifacts:
    - path: "api-spec/openapi.yaml"
      provides: "Unified API contract with headers"
    - path: "backend/src/main/java/de/goaldone/backend/security/TenantContextFilter.java"
      provides: "Header extraction and validation"
  key_links:
    - from: "TenantContextFilter"
      to: "TenantContext"
      via: "ThreadLocal storage"
---

<objective>
Establish the core API contract and backend infrastructure for header-based multi-tenancy. This removes legacy path parameters and introduces a centralized mechanism for organization context resolution.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-refine-organization-context-and-header-requirements/3.1-CONTEXT.md
@.planning/phases/03.1-refine-organization-context-and-header-requirements/03.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OpenAPI Specification</name>
  <files>api-spec/openapi.yaml</files>
  <action>
    - Remove `{accountId}` path parameter from all endpoints (D-04).
    - Remove `{orgId}` path parameter from paths, shifting to `X-Org-ID` header (D-05).
    - Update member management path to `/organization/members` (D-06).
    - Apply the `OrgIdHeader` component to all destructive/creative endpoints (POST, PUT, DELETE) and make it optional for listing endpoints (GET) (D-02, D-03).
    - Ensure `X-Org-ID` is defined in the components/headers or components/parameters section.
  </action>
  <verify>
    <automated>npx openapi-generator-cli validate -i api-spec/openapi.yaml</automated>
  </verify>
  <done>OpenAPI spec reflects header-based context and passes validation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TenantContext and Filter</name>
  <files>
    backend/src/main/java/de/goaldone/backend/security/TenantContext.java
    backend/src/main/java/de/goaldone/backend/security/TenantContextFilter.java
  </files>
  <action>
    - Create `TenantContext` using a `ThreadLocal<String>` to store the current `orgId`. Include `set()`, `get()`, and `clear()` methods.
    - Create `TenantContextFilter` (extending `OncePerRequestFilter`):
      - Extract `X-Org-ID` header.
      - Validate header: If present, verify it exists within the user's `orgs` claim in the JWT (D-08, D-09).
      - If invalid/mismatch, return `403 Forbidden` (D-09).
      - If valid, store in `TenantContext`.
      - Use a `try-finally` block to ensure `TenantContext.clear()` is called.
    - Register filter in `SecurityConfig` after `BearerTokenAuthenticationFilter`.
  </action>
  <verify>
    <automated>./mvnw clean compile</automated>
  </verify>
  <done>Tenant infrastructure correctly captures and validates the organization header.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Custom Security Expressions</name>
  <files>
    backend/src/main/java/de/goaldone/backend/security/expression/CustomMethodSecurityExpressionRoot.java
    backend/src/main/java/de/goaldone/backend/config/MethodSecurityConfig.java
  </files>
  <action>
    - Implement `hasOrgRole(String role)` in `CustomMethodSecurityExpressionRoot` (per D-07, D-08).
    - The logic should:
      1. Get `activeOrgId` from `TenantContext`.
      2. If null, return false (for destructive actions requiring context).
      3. Extract `orgs` claim from JWT.
      4. Verify a membership exists for `activeOrgId` with the specified `role`.
    - Register the custom `MethodSecurityExpressionHandler` in `MethodSecurityConfig`.
  </action>
  <verify>
    <automated>./mvnw clean compile</automated>
  </verify>
  <done>@PreAuthorize("hasOrgRole('...')") is functional and context-aware.</done>
</task>

</tasks>

<success_criteria>
- OpenAPI spec is updated and valid.
- Backend compiles with new security infrastructure.
- X-Org-ID header is successfully extracted and validated against JWT claims.
</success_criteria>
