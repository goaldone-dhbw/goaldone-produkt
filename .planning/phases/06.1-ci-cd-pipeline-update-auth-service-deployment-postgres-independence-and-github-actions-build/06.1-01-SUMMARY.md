# Plan 06.1-01 Summary: GitHub Actions — Auth-Service Build Job & Deploy Parameters

## Status: COMPLETE

## What Was Built

Updated `.github/workflows/cd.yml` to include auth-service as a first-class CI/CD citizen:

1. **Auth-Service Docker Build Step** — Added `Build and push auth-service image` step using `docker/build-push-action@v5` targeting `./auth-service/Dockerfile`. Tags pushed to `ghcr.io/goaldone-dhbw/goaldone-auth-service:VERSION` and `:latest`. Runs after frontend build (no path-based conditional per D-13).

2. **Version Tagging Unification** — Extended `Update version files` step to include `auth-service/pom.xml` sed replacement. Added `auth-service/pom.xml` to the git add/commit in `Commit and push version updates`. All three services (backend, frontend, auth-service) now versioned with the same semantic version tag (D-12).

3. **Deploy Job Updates** — Replaced Zitadel secrets with AUTH_SERVICE_* secrets in both deploy-dev and deploy-prod:
   - `envs` list: replaced `DEV/PROD_ZITADEL_CLIENT_ID` with `DEV/PROD_AUTH_SERVICE_RSA_PRIVATE_KEY`, `DEV/PROD_AUTH_SERVICE_DB_PASSWORD`, `DEV/PROD_AUTH_SERVICE_ISSUER_URI`
   - Added sed injection for all three AUTH_SERVICE_* variables into the VPS `.env` file
   - Updated `deploy.sh` call to pass `$VERSION $VERSION $VERSION` (backend, frontend, auth-service)

## Files Modified

- `.github/workflows/cd.yml` — Auth-service build step, version tagging, deploy secret injection

## Key Decisions Applied

- D-12: All three services use same semantic version tag
- D-13: Auth-service built on every master push (no path-based conditional)
- D-05: RSA private key injected from GitHub Secrets at deploy time

## Port Note

Auth-service Dockerfile exposes port **9000** (not 8080). Health checks in downstream plans use port 9000 accordingly.
