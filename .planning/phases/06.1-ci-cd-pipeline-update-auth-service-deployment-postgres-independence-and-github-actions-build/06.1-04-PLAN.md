---
phase: 06.1-ci-cd-pipeline-update-auth-service-deployment-postgres-independence-and-github-actions-build
plan: 04
type: execute
wave: 4
depends_on: ["06.1-03"]
files_modified:
  - .github/workflows/cd.yml
  - infra/infra-setup
  - docker/dev/.env.example
  - docker/prod/.env.example
autonomous: true
requirements: []
must_haves:
  truths:
    - "Zitadel infrastructure (infra/infra-setup/) completely removed"
    - ".env templates contain only AUTH_SERVICE_* variables, no ZITADEL_* references"
    - "GitHub Secrets used for auth-service key injection instead of Zitadel configuration"
    - "Frontend env.js template points to auth-service issuer (no Zitadel references)"
    - "Backend Spring profiles use auth-service issuer-uri (no Zitadel references)"
  artifacts:
    - path: ".github/workflows/cd.yml"
      provides: "CD pipeline without Zitadel setup, with auth-service secret injection"
      exports: ["GitHub Secrets injection for AUTH_SERVICE_RSA_PRIVATE_KEY"]
    - path: "docker/dev/.env.example"
      provides: "Dev env template with auth-service config only (Zitadel removed)"
      exports: ["No ZITADEL_* variables"]
    - path: "docker/prod/.env.example"
      provides: "Prod env template with auth-service config only (Zitadel removed)"
      exports: ["No ZITADEL_* variables"]
    - path: "infra/infra-setup"
      provides: "Removed directory (no artifact, but git status confirms deletion)"
      exports: []
  key_links:
    - from: ".github/workflows/cd.yml (deploy-dev/deploy-prod)"
      to: "DEV/PROD_AUTH_SERVICE_RSA_PRIVATE_KEY GitHub Secrets"
      via: "sed injection into .env before deploy.sh"
      pattern: "Secret management for auth-service RSA key (never committed)"
    - from: "docker/dev/.env.example"
      to: "(removed) ZITADEL_* variables"
      via: "cleanup — no longer used"
      pattern: "Complete cutover from Zitadel to auth-service"
---

<objective>
Clean up deployment configuration by removing all Zitadel infrastructure references and completing the cutover to auth-service. This includes removing the Zitadel infrastructure directory, cleaning .env templates, updating GitHub Secrets references, and ensuring all configuration points to auth-service.

Purpose: Achieve complete Zitadel removal (D-14) and finalize the configuration for auth-service deployment (D-09, D-10, D-11).

Output: Zitadel infrastructure removed, .env templates cleaned of Zitadel references, GitHub Actions updated to inject auth-service secrets, all configuration consistent with auth-service deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/06.1-ci-cd-pipeline-update-auth-service-deployment-postgres-independence-and-github-actions-build/06.1-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/cd.yml — CD pipeline with Zitadel/auth-service secret handling
@docker/dev/.env.example — Dev template (currently mixed Zitadel and auth-service)
@docker/prod/.env.example — Prod template (currently mixed Zitadel and auth-service)
@infra/infra-setup/ — Directory to be deleted
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Zitadel Variables from .env Templates</name>
  <files>docker/dev/.env.example, docker/prod/.env.example</files>
  <action>
    Remove all ZITADEL_* environment variables from both dev and prod .env.example files. These are no longer needed — complete cutover to auth-service.
    
    **Variables to remove from docker/dev/.env.example:**
    ```
    # ZITADEL OIDC
    ZITADEL_CLIENT_ID=YOUR_DEV_CLIENT_ID
    ZITADEL_GOALDONE_PROJECT_ID=YOUR_ZITADEL_PROJECT_ID
    ZITADEL_GOALDONE_ORG_ID=YOUR_ZITADEL_ORG_ID
    ZITADEL_SERVICE_ACCOUNT_TOKEN=YOUR_SERVICE_ACCOUNT_TOKEN
    ```
    
    **Variables to remove from docker/prod/.env.example:**
    Same ZITADEL_* variables (adjust for prod placeholders).
    
    **Why:** Zitadel is completely removed per D-14. Backend no longer validates against Zitadel issuer (uses auth-service instead). Frontend no longer uses Zitadel client ID (uses auth-service client ID).
    
    **Verification:** No ZITADEL_* variables remain in either template. AUTH_SERVICE_* variables already present from Plan 02.
    
    **Note:** This cleanup assumes Plans 01-03 have already added AUTH_SERVICE_* variables to the templates.
  </action>
  <verify>
    <automated>
      Verify ZITADEL_* variables removed from both templates:
      ! grep -q 'ZITADEL_' docker/dev/.env.example && ! grep -q 'ZITADEL_' docker/prod/.env.example && echo "PASS: ZITADEL_* variables removed from both templates"
    </automated>
  </verify>
  <done>
    - All ZITADEL_* variables removed from docker/dev/.env.example
    - All ZITADEL_* variables removed from docker/prod/.env.example
    - AUTH_SERVICE_* variables remain (from Plan 02)
    - Blue-green deployment variables remain (FRONTEND_BLUE_ACTIVE, etc.)
    - File structure clean and simplified
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GitHub Actions cd.yml — Remove Zitadel Secret Injection</name>
  <files>.github/workflows/cd.yml</files>
  <action>
    In both deploy-dev and deploy-prod jobs, remove the Zitadel secret injection logic. This includes:
    - Removing ZITADEL_* environment variables from the envs list
    - Removing sed commands that inject DEV_ZITADEL_* / PROD_ZITADEL_* secrets into .env
    
    **Current state (deploy-dev, lines 189-225):**
    ```bash
    envs: VERSION,GHCR_TOKEN,DEV_POSTGRES_PASSWORD,DEV_ZITADEL_CLIENT_ID
    ...
    if [ -n "$DEV_ZITADEL_CLIENT_ID" ]; then
      sed -i 's/^ZITADEL_CLIENT_ID=.*/ZITADEL_CLIENT_ID='"$DEV_ZITADEL_CLIENT_ID"'/' ~/docker/dev/.env
    fi
    ```
    
    **New state:**
    ```bash
    envs: VERSION,GHCR_TOKEN,DEV_POSTGRES_PASSWORD,DEV_AUTH_SERVICE_RSA_PRIVATE_KEY,DEV_AUTH_SERVICE_DB_PASSWORD,DEV_AUTH_SERVICE_ISSUER_URI
    ...
    # (no Zitadel sed commands)
    # (AUTH_SERVICE_* injection handled separately — from Plan 01, Task 3)
    ```
    
    **Why:** Zitadel secrets no longer needed. Auth-service RSA key, DB password, and issuer URI are injected via GitHub Secrets (per D-05, D-09, D-10).
    
    **Important:** This task assumes Plan 01, Task 3 already added AUTH_SERVICE_* environment variable injection. If not done, refer back to Plan 01.
    
    **Validation:** No ZITADEL_* variables remain in envs list or sed commands.
  </action>
  <verify>
    <automated>
      Verify Zitadel secrets removed from cd.yml:
      ! grep -q 'ZITADEL_CLIENT_ID' .github/workflows/cd.yml && ! grep -q 'DEV_ZITADEL' .github/workflows/cd.yml && echo "PASS: Zitadel secrets removed from cd.yml"
    </automated>
  </verify>
  <done>
    - deploy-dev envs list no longer includes ZITADEL_* variables
    - deploy-prod envs list no longer includes ZITADEL_* variables
    - sed commands for ZITADEL_* removed from both deploy jobs
    - AUTH_SERVICE_* injection logic remains (from Plan 01)
    - No references to Zitadel configuration in workflow
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete infra/infra-setup Directory (Zitadel Infrastructure)</name>
  <files>infra/infra-setup (entire directory)</files>
  <action>
    Delete the entire `infra/infra-setup/` directory and all its contents. This directory contains the Zitadel + PostgreSQL docker-compose configuration, which is no longer needed — auth-service and backends now use docker-compose in docker/dev/ and docker/prod/.
    
    **Current directory structure (example):**
    ```
    infra/infra-setup/
    ├── docker-compose.yml     (Zitadel + Postgres setup)
    ├── .env.example            (Zitadel environment)
    ├── LICENSE                 (if present)
    └── README.md               (if present)
    ```
    
    **Action:** Delete entire directory and commit the removal.
    
    **Why:** Per D-14: "Complete cutover to auth-service — no dual-IdP period or fallback to Zitadel." Zitadel infrastructure is completely removed from the monorepo.
    
    **Impact:** Developers running the full stack locally will now use docker-compose from docker/dev/ only (which now includes auth-service per Plan 02). No need for separate Zitadel setup.
    
    **Validation:** Directory deleted and git status shows removal.
  </action>
  <verify>
    <automated>
      Verify infra/infra-setup directory is deleted:
      ! [ -d infra/infra-setup ] && echo "PASS: infra/infra-setup directory deleted"
    </automated>
  </verify>
  <done>
    - infra/infra-setup/ directory completely deleted
    - All Zitadel infrastructure files removed from repository
    - git status will show directory deletion
  </done>
</task>

<task type="auto">
  <name>Task 4: Update Backend Spring Profile Configuration Documentation</name>
  <files>docker/dev/.env.example, docker/prod/.env.example</files>
  <action>
    Add comments to the .env templates documenting that backend and auth-service use auth-service issuer URIs (not Zitadel). This is purely documentation for operators setting up new environments.
    
    **In docker/dev/.env.example, add a comment before AUTH_SERVICE section:**
    ```bash
    # ============================================================================
    # Auth-Service Configuration
    # Note: All services authenticate against auth-service OIDC endpoint
    # Backend validates JWT from AUTH_SERVICE_JWT_ISSUER_URI
    # Frontend exchanges auth code at AUTH_SERVICE_JWT_ISSUER_URI
    # ============================================================================
    AUTH_SERVICE_VERSION=0.0.1
    ...
    ```
    
    **Similarly in docker/prod/.env.example** (same comment block).
    
    **Why:** Clarity for future operators — the migration from Zitadel to auth-service is now complete, and all configuration points to auth-service (D-14).
    
    **Validation:** Comments added, no functional changes.
  </action>
  <verify>
    <automated>
      Verify auth-service documentation comment exists in .env templates:
      grep -q 'Auth-service OIDC endpoint' docker/dev/.env.example && echo "PASS: Auth-service documentation comment added"
    </automated>
  </verify>
  <done>
    - Comment block added to docker/dev/.env.example explaining auth-service as OIDC provider
    - Same comment block added to docker/prod/.env.example
    - No functional changes, purely documentation
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify No Zitadel References Remain in GitHub Actions Workflow</name>
  <files>.github/workflows/cd.yml</files>
  <action>
    Final cleanup: ensure no Zitadel references remain anywhere in the cd.yml workflow file that would expect Zitadel infrastructure.
    
    **Scan for and remove any:**
    - ZITADEL_* environment variable references
    - Comments mentioning Zitadel setup
    - Any docker pull or start commands for Zitadel images
    - Any conditional steps or secrets related to Zitadel
    
    **Note:** This is a verification/cleanup task following Tasks 1-2. If you find any Zitadel references, remove them.
    
    **Why:** Ensure complete removal and prevent accidental re-introduction of Zitadel dependencies.
    
    **Validation:** No grep matches for 'zitadel' (case-insensitive) in cd.yml.
  </action>
  <verify>
    <automated>
      Verify no Zitadel references in cd.yml:
      ! grep -i 'zitadel' .github/workflows/cd.yml && echo "PASS: No Zitadel references in cd.yml"
    </automated>
  </verify>
  <done>
    - All Zitadel references removed from .github/workflows/cd.yml
    - Workflow only references auth-service, backend, frontend (no Zitadel)
    - No stray comments or conditional logic mentioning Zitadel
  </done>
</task>

</tasks>

<verification>
**After Plan 04 completes:**
- [ ] docker/dev/.env.example has no ZITADEL_* variables (only AUTH_SERVICE_*)
- [ ] docker/prod/.env.example has no ZITADEL_* variables (only AUTH_SERVICE_*)
- [ ] .github/workflows/cd.yml has no ZITADEL_* in envs lists
- [ ] .github/workflows/cd.yml has no sed commands for ZITADEL_* variables
- [ ] infra/infra-setup/ directory deleted from git
- [ ] grep -i 'zitadel' .github/workflows/cd.yml returns no matches
- [ ] Documentation comments in .env templates clarify auth-service as OIDC provider
- [ ] Phase 6.1 complete: All deliverables across all 4 plans ready for testing
</verification>

<success_criteria>
Zitadel completely removed from codebase (D-14). All environment templates reference only auth-service configuration. GitHub Actions workflow contains no Zitadel secrets or logic. Infra directory cleaned. Complete cutover from Zitadel to auth-service is finalized and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-ci-cd-pipeline-update-auth-service-deployment-postgres-independence-and-github-actions-build/06.1-04-SUMMARY.md`
</output>
