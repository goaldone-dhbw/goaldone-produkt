---
status: complete
phase: 06.1-ci-cd-pipeline-update-auth-service-deployment-postgres-independence-and-github-actions-build
source:
  - 06.1-01-SUMMARY.md
  - 06.1-02-SUMMARY.md
  - 06.1-03-SUMMARY.md
  - 06.1-04-SUMMARY.md
started: "2026-05-03T16:41:34.104+02:00"
updated: "2026-05-03T16:55:00.000+02:00"
---

## Current Test

[testing complete]

## Tests

### 1. Auth-Service Docker Build Step in cd.yml
expected: .github/workflows/cd.yml contains a "Build and push auth-service image" step using docker/build-push-action@v5 targeting ./auth-service/Dockerfile, pushing to ghcr.io/goaldone-dhbw/goaldone-auth-service:VERSION and :latest. No path-based conditionals — builds on every master push.
result: pass

### 2. Auth-Service Version Tagging in cd.yml
expected: The "Update version files" step in cd.yml runs sed on auth-service/pom.xml (same pattern as backend/pom.xml), and auth-service/pom.xml is included in the git add/commit alongside backend/pom.xml and frontend/package.json.
result: pass

### 3. AUTH_SERVICE_* Secrets in Deploy Jobs
expected: Both deploy-dev and deploy-prod jobs in cd.yml reference DEV/PROD_AUTH_SERVICE_RSA_PRIVATE_KEY, DEV/PROD_AUTH_SERVICE_DB_PASSWORD, and DEV/PROD_AUTH_SERVICE_ISSUER_URI (not Zitadel secrets). deploy.sh is called with $VERSION three times (backend, frontend, auth-service).
result: pass

### 4. postgres-auth and auth-service Services in docker-compose (dev)
expected: docker/dev/docker-compose.yaml contains postgres-auth-dev and auth-service-dev services. postgres-auth-dev has a healthcheck. auth-service-dev depends_on postgres-auth-dev with condition: service_healthy. auth-service-dev has a healthcheck on port 9000.
result: pass

### 5. Backend Startup Order Enforced in docker-compose (dev)
expected: backend-dev's depends_on block includes both postgres-dev and auth-service-dev, each with condition: service_healthy. This ensures auth-service is ready before backend starts (D-03).
result: pass

### 6. Dev docker-compose Mirrors to Prod
expected: docker/prod/docker-compose.yaml has postgres-auth-prod and auth-service-prod with the same structure as dev (service_healthy conditions, port 9000 healthcheck), plus postgres-auth-prod-data volume. backend-prod depends on auth-service-prod.
result: pass

### 7. .env.example Files Updated (No Zitadel, Has AUTH_SERVICE_*)
expected: Both docker/dev/.env.example and docker/prod/.env.example contain AUTH_SERVICE_VERSION, AUTH_SERVICE_DB_PASSWORD, AUTH_SERVICE_RSA_PRIVATE_KEY, AUTH_SERVICE_JWT_ISSUER_URI. No ZITADEL_* variables remain. Prod issuer URI uses https://auth.goaldone.de.
result: pass

### 8. deploy.sh Accepts Auth-Service Version
expected: scripts/deploy.sh accepts 4 parameters. AUTH_SVC_VER=$4 is assigned and printed at startup. The pull/startup/health-check sequence runs in order: postgres-auth → auth-service → postgres → backend → frontend.
result: pass

### 9. JWKS Endpoint Validation in deploy.sh
expected: deploy.sh contains a curl check against http://auth-service-${STAGE}:9000/oauth2/jwks before proceeding to backend startup. Script exits with code 1 if JWKS is unreachable. Auth-Service version appears in the deployment summary output.
result: pass

### 10. Zitadel Infrastructure Removed
expected: The infra/infra-setup/ directory no longer exists in the repository. No ZITADEL_* references remain in .github/workflows/cd.yml. docker-compose files have no ZITADEL_* environment variable entries (the "zitadel" network name is a Traefik infrastructure name and is expected to remain).
result: pass

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0

## Gaps

[none]

