---
phase: 02-backend-jwt-validation
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - backend/src/main/java/de/goaldone/backend/config/SecurityConfig.java
  - backend/src/test/java/de/goaldone/backend/util/SecurityTestUtil.java
autonomous: true
requirements: [JWT-04, JWT-05, TEST-01, TEST-02]
must_haves:
  truths:
    - "Centralized authorization logic verifies orgId match (per D-05)"
    - "Integration tests pass using the new auth-service claim shapes (per D-06)"
    - "Test helpers generate tokens with authorities, user_id, and orgs claims"
  artifacts:
    - path: "backend/src/main/java/de/goaldone/backend/config/SecurityConfig.java"
      provides: "Centralized Authz registration"
    - path: "backend/src/test/java/de/goaldone/backend/util/SecurityTestUtil.java"
      provides: "Updated test helpers"
  key_links:
    - from: "Integration Tests"
      to: "Backend API"
      via: "MockMvc with updated JWT helpers"
---

<objective>
Implement centralized multi-org authorization logic and update test helpers to match the new token structure (per D-05, D-06).
Ensure RBAC and organization-level access control work end-to-end.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-backend-jwt-validation/02-CONTEXT.md
@.planning/phases/02-backend-jwt-validation/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Centralized Multi-Org Authorization</name>
  <files>backend/src/main/java/de/goaldone/backend/config/SecurityConfig.java</files>
  <action>
    - Introduce a centralized mechanism (e.g., a custom Security Expression or a SpEL-based check in `@PreAuthorize`) to verify org access.
    - The check should verify that the `orgId` (e.g., from path variable or request param) matches one of the organization IDs in the `orgs` claim of the current JWT.
    - Implement a method/bean that can be used like `@PreAuthorize("@authz.isMember(#orgId)")`.
    - Handle global `ROLE_ADMIN` (super admin) which might bypass this check (per specifics in 02-CONTEXT).
  </action>
  <verify>
    <automated>./mvnw test -Dtest=MultiOrgAuthorizationTest</automated>
  </verify>
  <done>Centralized organization membership check implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Update Test Helpers and Integration Tests</name>
  <files>backend/src/test/java/de/goaldone/backend/util/SecurityTestUtil.java</files>
  <action>
    - Update `SecurityTestUtil` (or equivalent test helper) to generate JWTs with the new claim shape:
        - `user_id` (UUID)
        - `authorities` (flat list of strings)
        - `orgs` (list of objects with id and name)
    - Ensure `MockMvc` security helpers (like `.with(jwt().authorities(...))`) are used correctly and match the new `JwtAuthenticationConverter` logic.
    - Update existing integration tests that were relying on Zitadel claims to use these new helpers.
    - Run all backend integration tests to ensure no regressions.
  </action>
  <verify>
    <automated>./mvnw test</automated>
  </verify>
  <done>Test helpers and integration tests updated; all tests pass.</done>
</task>

</tasks>

<success_criteria>
- Users can only access data belonging to organizations they are members of.
- Global admins can still access all organizations.
- All backend integration tests pass with the new security model.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-jwt-validation/02-04-SUMMARY.md`
</output>
