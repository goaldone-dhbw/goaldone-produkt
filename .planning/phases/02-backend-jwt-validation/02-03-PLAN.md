---
phase: 02-backend-jwt-validation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - backend/src/main/java/de/goaldone/backend/service/JitProvisioningService.java
  - backend/src/main/java/de/goaldone/backend/filter/JitProvisioningFilter.java
autonomous: true
requirements: [JIT-01, JIT-02, JIT-03, JIT-04]
must_haves:
  truths:
    - "JIT Provisioning handles bulk memberships from the 'orgs' claim (per D-04)"
    - "Provisioning is idempotent and does not create duplicate memberships"
    - "New users are provisioned using the 'user_id' claim"
  artifacts:
    - path: "backend/src/main/java/de/goaldone/backend/service/JitProvisioningService.java"
      provides: "Bulk JIT provisioning logic"
  key_links:
    - from: "JitProvisioningFilter"
      to: "JitProvisioningService"
      via: "filter chain entry"
---

<objective>
Update JIT provisioning logic to handle the multi-org 'orgs' claim and ensure all memberships listed in the token are provisioned in the local database (per D-04).
Ensure the logic uses the new 'user_id' and 'orgs' claims.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-backend-jwt-validation/02-CONTEXT.md
@.planning/phases/02-backend-jwt-validation/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update JitProvisioningService for Bulk Provisioning</name>
  <files>backend/src/main/java/de/goaldone/backend/service/JitProvisioningService.java</files>
  <action>
    - Refactor `provisionUser` to handle the new `user_id` and `orgs` claims.
    - Implement logic to iterate through the `orgs` array (list of `{"id": "...", "name": "..."}`).
    - For each organization in the array:
        - Ensure the `OrganizationEntity` exists (provision if missing).
        - Ensure the membership for the user exists in that organization.
    - Ensure the logic is idempotent (checks for existence before creation).
    - Use the new `authUserId` and `authCompanyId` fields for lookups.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=JitProvisioningServiceTest</automated>
  </verify>
  <done>Bulk JIT provisioning implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Update JitProvisioningFilter</name>
  <files>backend/src/main/java/de/goaldone/backend/filter/JitProvisioningFilter.java</files>
  <action>
    - Update the filter to extract the `user_id` and `orgs` claims from the `JwtAuthenticationToken`.
    - Pass these claims to the updated `JitProvisioningService`.
    - Ensure the filter handles cases where claims might be missing gracefully (log warning, continue).
  </action>
  <verify>
    <automated>./mvnw clean compile</automated>
  </verify>
  <done>JitProvisioningFilter updated to bridge JWT claims to the service.</done>
</task>

</tasks>

<success_criteria>
- JIT provisioning correctly handles multiple organizations for a single user.
- New users and organizations are auto-provisioned on first login.
- No duplicate records are created on subsequent logins.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-jwt-validation/02-03-SUMMARY.md`
</output>
