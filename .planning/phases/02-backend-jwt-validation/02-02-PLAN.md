---
phase: 02-backend-jwt-validation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/src/main/java/de/goaldone/backend/config/SecurityConfig.java
  - backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java
autonomous: true
requirements: [JWT-01, JWT-02, JWT-03, DB-06]
must_haves:
  truths:
    - "Backend extracts roles from the flat 'authorities' claim (per D-01)"
    - "Extracted roles have the 'ROLE_' prefix (per D-02)"
    - "CurrentUserResolver correctly identifies users via authUserId (per D-03/DB-06)"
  artifacts:
    - path: "backend/src/main/java/de/goaldone/backend/config/SecurityConfig.java"
      provides: "Updated JWT claim mapping"
    - path: "backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java"
      provides: "Updated user resolution"
  key_links:
    - from: "JwtAuthenticationConverter"
      to: "authorities claim"
      via: "claim extraction logic"
---

<objective>
Rewire Spring Security to extract authorities from the new flat 'authorities' claim and apply the 'ROLE_' prefix.
Update the CurrentUserResolver to use the renamed identifier fields.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-backend-jwt-validation/02-CONTEXT.md
@.planning/phases/02-backend-jwt-validation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SecurityConfig and JwtAuthenticationConverter</name>
  <files>backend/src/main/java/de/goaldone/backend/config/SecurityConfig.java</files>
  <action>
    - Remove the complex Zitadel role mapping from `JwtAuthenticationConverter`.
    - Implement a new mapping that reads the `authorities` claim as a `List<String>`.
    - Apply the `ROLE_` prefix to each string in the `authorities` claim (per D-02).
    - Ensure the `JwtAuthenticationConverter` is correctly registered in the `SecurityFilterChain`.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=SecurityConfigTest</automated>
  </verify>
  <done>Authorities mapping updated to new flat claim shape with ROLE_ prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Update CurrentUserResolver</name>
  <files>backend/src/main/java/de/goaldone/backend/service/CurrentUserResolver.java</files>
  <action>
    - Update the user resolution logic to use the `authUserId` claim (from JWT `sub` or `user_id`) to find the `UserAccountEntity`.
    - Ensure it uses the renamed `findByAuthUserId` method in `UserAccountRepository`.
    - Remove any Zitadel-specific fallback logic.
  </action>
  <verify>
    <automated>./mvnw clean compile</automated>
  </verify>
  <done>CurrentUserResolver updated to use new identifier fields.</done>
</task>

</tasks>

<success_criteria>
- Backend extracts authorities correctly from the new token format.
- Users are correctly resolved via their authUserId.
- Project compiles and security tests (to be updated later) pass for basic compilation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-jwt-validation/02-02-SUMMARY.md`
</output>
