---
phase: 02-backend-jwt-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/resources/db/changelog/db.changelog-master.xml
  - backend/src/main/resources/db/changelog/changes/010-rename-zitadel-identifiers.xml
  - backend/src/main/java/de/goaldone/backend/entity/UserAccountEntity.java
  - backend/src/main/java/de/goaldone/backend/entity/OrganizationEntity.java
  - backend/src/main/java/de/goaldone/backend/repository/UserAccountRepository.java
  - backend/src/main/java/de/goaldone/backend/repository/OrganizationRepository.java
autonomous: true
requirements: [DB-01, DB-02, DB-03, DB-05]
must_haves:
  truths:
    - "Database columns zitadel_sub and zitadel_org_id are renamed to auth_user_id and auth_company_id"
    - "Entity fields zitadelSub and zitadelOrgId are renamed to authUserId and authCompanyId"
    - "Repository methods use the new field names (e.g., findByAuthUserId)"
  artifacts:
    - path: "backend/src/main/resources/db/changelog/changes/010-rename-zitadel-identifiers.xml"
      provides: "Liquibase migration for renaming"
    - path: "backend/src/main/java/de/goaldone/backend/entity/UserAccountEntity.java"
      provides: "Refactored user entity"
    - path: "backend/src/main/java/de/goaldone/backend/repository/UserAccountRepository.java"
      provides: "Refactored user repository"
  key_links:
    - from: "backend/src/main/java/de/goaldone/backend/entity/UserAccountEntity.java"
      to: "database table"
      via: "JPA @Column annotation"
---

<objective>
Rename Zitadel-specific identifiers in the database and entities to align with the new auth model (per D-03).
This removes technical debt early and prepares the codebase for the new JWT claim shape.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-backend-jwt-validation/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Liquibase migration to rename columns</name>
  <files>
    backend/src/main/resources/db/changelog/db.changelog-master.xml,
    backend/src/main/resources/db/changelog/changes/010-rename-zitadel-identifiers.xml
  </files>
  <action>
    - Create a new Liquibase changeset `010-rename-zitadel-identifiers.xml`.
    - Rename column `zitadel_sub` to `auth_user_id` in `user_accounts` table.
    - Rename column `zitadel_org_id` to `auth_company_id` in `organizations` table.
    - Update `db.changelog-master.xml` to include the new file.
    - Ensure unique constraints and indexes are preserved or updated.
  </action>
  <verify>
    <automated>./mvnw compile liquibase:update -Dspring.profiles.active=local</automated>
  </verify>
  <done>Database schema updated; columns renamed successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Entities and Repositories</name>
  <files>
    backend/src/main/java/de/goaldone/backend/entity/UserAccountEntity.java,
    backend/src/main/java/de/goaldone/backend/entity/OrganizationEntity.java,
    backend/src/main/java/de/goaldone/backend/repository/UserAccountRepository.java,
    backend/src/main/java/de/goaldone/backend/repository/OrganizationRepository.java
  </files>
  <action>
    - In `UserAccountEntity`: Rename `zitadelSub` field to `authUserId`. Update `@Column(name = "zitadel_sub")` to `@Column(name = "auth_user_id")`.
    - In `OrganizationEntity`: Rename `zitadelOrgId` field to `authCompanyId`. Update `@Column(name = "zitadel_org_id")` to `@Column(name = "auth_company_id")`.
    - Update all references in `UserAccountRepository` (e.g., `findByZitadelSub` -> `findByAuthUserId`).
    - Update all references in `OrganizationRepository` (e.g., `findByZitadelOrgId` -> `findByAuthCompanyId`).
    - Fix any broken references in the project.
  </action>
  <verify>
    <automated>./mvnw clean compile</automated>
  </verify>
  <done>Codebase refactored to use new identifier names; project compiles.</done>
</task>

</tasks>

<success_criteria>
- Liquibase migration applied successfully.
- All entities and repositories use the new identifier names.
- Backend project compiles without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-jwt-validation/02-01-SUMMARY.md`
</output>
