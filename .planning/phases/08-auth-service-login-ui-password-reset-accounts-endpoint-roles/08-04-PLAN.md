---
phase: "08"
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - auth-service/src/main/resources/templates/login.html
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03

must_haves:
  truths:
    - "login.html uses fragments/layout.html (same visual wrapper as forgot-password and reset-password pages)"
    - "login.html has NO inline gradient background (linear-gradient removed)"
    - "login.html has NO standalone <style> block with color/font declarations"
    - "login.html has a 'Forgot your password?' link pointing to /forgot-password"
    - "login.html has a password visibility toggle button with aria-label"
    - "login.html shows error message using .alert .alert-error on invalid credentials"
    - "login.html title is 'Sign In – GoalDone'"
    - "Login form still works — th:action='@{/login}' with username and password fields"
    - "Remember-me checkbox is removed"
  artifacts:
    - path: "auth-service/src/main/resources/templates/login.html"
      provides: "Redesigned login page using fragments/layout.html + base.css"
      contains: "layout:decorate"

# D-02 DEVIATION ACKNOWLEDGED (same as Plan 03):
# Using the existing GoalDone base.css design system instead of Bootstrap/Tailwind.
# The existing system IS a production-quality design framework — see 08-03-PLAN.md for full rationale.
---

<objective>
Redesign `login.html` to use `fragments/layout.html` and `base.css` — making it visually consistent with the forgot-password and reset-password pages. Add password visibility toggle and "Forgot your password?" link. Remove the gradient background and "remember me" checkbox.

**D-01/D-02/D-03:** Production-ready login page redesign using the existing GoalDone CSS design system (`base.css`). D-02 intent (consistent, professional appearance, lightweight framework) is satisfied by the existing `base.css` — no Bootstrap/Tailwind introduced (see 08-03-PLAN.md for full D-02 deviation rationale).

Purpose: Complete the auth-service login page redesign to match GoalDone branding and be visually consistent with other auth pages.

Output:
- `login.html` — completely rewritten to use `layout:decorate="~{fragments/layout}"`, base.css classes, password toggle, forgot-password link
- Manual checkpoint confirming login page renders correctly in browser
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-auth-service-login-ui-password-reset-accounts-endpoint-roles/08-CONTEXT.md
@.planning/phases/08-auth-service-login-ui-password-reset-accounts-endpoint-roles/08-UI-SPEC.md

<interfaces>
<!-- Current login.html state — key facts the executor needs to know -->

Current login.html (to be replaced):
- Has a standalone inline <style> block (lines 7-113) — includes gradient background, custom button, custom inputs
- Does NOT use fragments/layout.html (inconsistent with reset/forgot pages)
- Form: th:action="@{/login}" method="post" — KEEP THIS
- Fields: username (type="text") and password (type="password") — KEEP THESE
- Error display: `th:if="${param.error}"` — currently uses `.error` class (custom); change to `.alert .alert-error`
- Has a "Remember me" checkbox — REMOVE per UI-SPEC (deferred feature)
- Does NOT have "Forgot your password?" link — ADD
- Does NOT have password toggle — ADD

fragments/layout.html provides:
- GoalDone header bar (brand name "Goaldone")
- `.container` (max-width 480px, centered, white card, shadow)
- body background via base.css (--bg-main: #f7fafc)
- Page content is placed inside `<div layout:fragment="content">`
- Scripts can be placed inside `<th:block layout:fragment="scripts">`

base.css classes available (verified from RESEARCH.md):
- `.btn .btn-primary` — primary button (background: --primary-500 #63729c)
- `.form-group` — form field wrapper
- `.form-label` — 14px, 500 weight label
- `.form-input` — input field with border, focus ring
- `.alert .alert-error` — error state box (red left border)
- `.alert .alert-info` — info state box (blue left border)
- `.text-muted` — muted text color (--text-muted: #718096)
- Spacing: `--space-2` (8px), `--space-4` (16px), `--space-6` (24px)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite login.html using fragments/layout.html + base.css</name>
  <files>auth-service/src/main/resources/templates/login.html</files>
  <read_first>
    - auth-service/src/main/resources/templates/login.html (full file — understand current structure, form action, field names)
    - auth-service/src/main/resources/templates/auth/forgot-password.html (full file — use as reference for correct layout:decorate pattern + base.css class usage)
    - auth-service/src/main/resources/templates/fragments/layout.html (skim — confirm layout:fragment="content" is the insertion point)
    - .planning/phases/08-auth-service-login-ui-password-reset-accounts-endpoint-roles/08-UI-SPEC.md (Page 1: login.html — elements list, Interaction Contracts: Password Visibility Toggle, Copywriting Contract: login.html)
  </read_first>
  <action>
Replace the entire content of `login.html` with:

```html
<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="~{fragments/layout}">
<head>
    <title>Sign In – GoalDone</title>
</head>
<body>
<div layout:fragment="content">
    <h2>Sign in to your account</h2>

    <!-- Error alert — invalid credentials -->
    <div th:if="${param.error}" class="alert alert-error" style="margin-bottom: var(--space-4);">
        <p style="margin: 0;">Invalid email or password. Please try again.</p>
    </div>

    <form th:action="@{/login}" method="post">
        <!-- Email / Username field -->
        <div class="form-group">
            <label for="username" class="form-label">Email or Username</label>
            <input type="text" id="username" name="username" class="form-input"
                   autofocus required style="width: 100%;">
        </div>

        <!-- Password field with visibility toggle -->
        <div class="form-group" style="position: relative;">
            <label for="password" class="form-label">Password</label>
            <input type="password" id="password" name="password" class="form-input"
                   required style="width: 100%;">
            <button type="button"
                    onclick="togglePassword('password', this)"
                    style="position: absolute; right: 10px; top: 32px; background: none; border: none; cursor: pointer; padding: 4px; color: var(--text-muted);"
                    aria-label="Show password">👁</button>
        </div>

        <!-- Forgot password link -->
        <div style="text-align: right; margin-bottom: var(--space-4);">
            <a th:href="@{/forgot-password}"
               style="color: var(--primary-500); font-size: 0.875rem; text-decoration: none;">
                Forgot your password?
            </a>
        </div>

        <!-- Submit button -->
        <button type="submit" class="btn btn-primary" style="width: 100%;">Sign In</button>
    </form>
</div>
<th:block layout:fragment="scripts">
<script>
function togglePassword(fieldId, btn) {
    var input = document.getElementById(fieldId);
    var isHidden = input.type === 'password';
    input.type = isHidden ? 'text' : 'password';
    btn.textContent = isHidden ? '🙈' : '👁';
    btn.setAttribute('aria-label', isHidden ? 'Hide password' : 'Show password');
}
</script>
</th:block>
</body>
</html>
```

Key invariants to verify after writing:
1. The file does NOT contain `linear-gradient` anywhere
2. The file does NOT contain a `<style>` block
3. The file contains `layout:decorate="~{fragments/layout}"`
4. The file contains `th:action="@{/login}"` (form still works)
5. The file contains `name="username"` and `name="password"` (Spring Security field names)
6. The file does NOT contain `remember-me` (removed)
7. The file contains `th:href="@{/forgot-password}"`
8. The file contains `togglePassword`
9. The file contains `aria-label="Show password"`
  </action>
  <verify>
    <automated>
      $f = "auth-service/src/main/resources/templates/login.html"
      $noGradient = (Select-String -Path $f -Pattern "linear-gradient" | Measure-Object).Count -eq 0
      $hasLayout = (Select-String -Path $f -Pattern "fragments/layout" | Measure-Object).Count -gt 0
      $hasToggle = (Select-String -Path $f -Pattern "togglePassword" | Measure-Object).Count -gt 0
      $hasForgot = (Select-String -Path $f -Pattern "forgot-password" | Measure-Object).Count -gt 0
      $hasFormAction = (Select-String -Path $f -Pattern 'th:action="@{/login}"' | Measure-Object).Count -gt 0
      "noGradient=$noGradient hasLayout=$hasLayout hasToggle=$hasToggle hasForgot=$hasForgot hasFormAction=$hasFormAction"
    </automated>
  </verify>
  <acceptance_criteria>
    - `login.html` does NOT contain `linear-gradient`
    - `login.html` does NOT contain a standalone `<style>` block with color/font declarations
    - `login.html` contains `layout:decorate="~{fragments/layout}"`
    - `login.html` contains `th:action="@{/login}"`
    - `login.html` contains `name="username"` and `name="password"` (Spring Security field names preserved)
    - `login.html` contains `th:href="@{/forgot-password}"`
    - `login.html` contains `togglePassword`
    - `login.html` contains `aria-label="Show password"` (accessibility)
    - `login.html` contains `class="btn btn-primary"` (submit button)
    - `login.html` contains `class="alert alert-error"` (error display)
    - `login.html` contains `Sign in to your account` (h2 heading — exact text per UI-SPEC)
    - `login.html` does NOT contain `remember-me` (checkbox removed)
    - `login.html` title contains `Sign In – GoalDone`
    - `cd auth-service && ./mvnw test -pl . -q` exits 0 — no template rendering tests broken
  </acceptance_criteria>
  <done>login.html rewritten: layout:decorate applied, gradient removed, forgot-password link added, password toggle added, base.css classes used throughout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Login page has been rewritten to use fragments/layout.html and base.css. The page now:
    - Uses GoalDone header + container layout (same as forgot-password and reset-password pages)
    - Shows "Sign in to your account" heading
    - Has email/username and password fields with base.css form styles
    - Has password visibility toggle (👁 button)
    - Has "Forgot your password?" link below the password field
    - Shows error message in .alert-error box on invalid credentials
    - Removed: gradient background, custom inline styles, "remember me" checkbox
  </what-built>
  <how-to-verify>
    1. Start the auth-service locally:
       ```bash
       cd auth-service && ./mvnw spring-boot:run -Dspring-boot.run.profiles=local
       ```
    2. Open http://localhost:9000/login in your browser

    **Visual checks:**
    - [ ] Page has GoalDone header bar (brand name visible)
    - [ ] No purple gradient background — background is light gray (#f7fafc)
    - [ ] Login form is in a white card/container (matches forgot-password page)
    - [ ] "Sign in to your account" heading visible
    - [ ] Password field has an 👁 toggle button on the right
    - [ ] "Forgot your password?" link visible below the password field
    - [ ] No "Remember me" checkbox

    **Functional checks:**
    - [ ] Click 👁 button → password characters become visible; button changes to 🙈
    - [ ] Click 🙈 button → password characters hidden again; button changes back to 👁
    - [ ] Click "Forgot your password?" → navigates to http://localhost:9000/forgot-password
    - [ ] Submit with wrong credentials → error message "Invalid email or password. Please try again."
    - [ ] Submit with correct credentials → redirected (OIDC flow proceeds normally)
  </how-to-verify>
  <resume-signal>Type "approved" if all visual and functional checks pass. Describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser → auth-service login page | Form POST to /login; username and password in POST body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Information Disclosure | login.html error message | accept | Error message "Invalid email or password. Please try again." is intentionally generic — does not distinguish between "user not found" and "wrong password" to prevent username enumeration. Already implemented in Spring Security form login. |
| T-08-04-02 | Spoofing | Password visibility toggle | accept | Toggle is client-side only (vanilla JS); password is revealed locally in the user's browser. No security impact — same pattern used by all major auth UIs. |
| T-08-04-03 | Tampering | Removed remember-me checkbox | accept | Remember-me was present in old login.html but is a deferred feature (CONTEXT.md deferred ideas). Removal does not affect current session management (Spring Session via JDBC/Redis). |
</threat_model>

<verification>
After manual approval, run the auth-service test suite to confirm no regressions:

```powershell
cd auth-service && ./mvnw test -q
```

Expected: BUILD SUCCESS.

Template invariant checks:
```powershell
# Confirm no gradient
Select-String -Path "auth-service/src/main/resources/templates/login.html" -Pattern "linear-gradient"
# Expected: no output

# Confirm layout decorator present
Select-String -Path "auth-service/src/main/resources/templates/login.html" -Pattern "fragments/layout"
# Expected: one match
```
</verification>

<success_criteria>
- login.html uses `layout:decorate="~{fragments/layout}"` — visually consistent with reset/forgot pages (D-01)
- No inline gradient background — replaced by base.css --bg-main
- "Forgot your password?" link navigates to /forgot-password (D-04 support)
- Password toggle works: 👁/🙈 toggles field type password ↔ text
- Error message "Invalid email or password" shows on failed login attempt
- All auth-service tests pass — no regressions
- Manual checkpoint approved: visual appearance matches GoalDone design system
</success_criteria>

<output>
After completion, create `.planning/phases/08-auth-service-login-ui-password-reset-accounts-endpoint-roles/08-04-SUMMARY.md`
</output>
