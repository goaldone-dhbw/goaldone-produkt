# ============================================================
# ZITADEL + Traefik — Environment Configuration
# ============================================================
# Copy this file to .env and adjust the values below.
#
#   cp .env-example .env
#
# Then generate secure secrets:
#
#   ZITADEL_MASTERKEY=$(tr -dc A-Za-z0-9 </dev/urandom | head -c 32)
#   POSTGRES_ADMIN_PASSWORD=$(tr -dc A-Za-z0-9 </dev/urandom | head -c 32)
#   POSTGRES_ZITADEL_PASSWORD=$(tr -dc A-Za-z0-9 </dev/urandom | head -c 32)
#
# Start:
#   docker compose up -d --wait
#
# Login at:
#   https://sso.goaldone.de/ui/console
# ============================================================

# -----------------------------
# Domain and external URL
# -----------------------------
ZITADEL_DOMAIN=sso.goaldone.de
ZITADEL_EXTERNALPORT=443
ZITADEL_EXTERNALSECURE=true

# -----------------------------
# Let's Encrypt
# -----------------------------
LETSENCRYPT_EMAIL=admin@goaldone.de

# -----------------------------
# Security / bootstrap
# -----------------------------
# Must be exactly 32 characters.
ZITADEL_MASTERKEY=MasterkeyNeedsToHave32Characters

# Login client PAT expiration (far future)
LOGIN_CLIENT_PAT_EXPIRATION=2099-01-01T00:00:00Z

# -----------------------------
# First instance admin user
# (only applied on first start)
# -----------------------------
ZITADEL_ORG_NAME=GoalDone
ZITADEL_ADMIN_USERNAME=admin
ZITADEL_ADMIN_PASSWORD=examplePass
ZITADEL_ADMIN_EMAIL=admin@goaldone.de

# -----------------------------
# Pinned image tags
# -----------------------------
ZITADEL_VERSION=v4.11.0
TRAEFIK_IMAGE=traefik:v3.6.8
POSTGRES_IMAGE=postgres:17.2-alpine

# -----------------------------
# Proxy settings
# -----------------------------
TRAEFIK_DASHBOARD_ENABLED=false
TRAEFIK_LOG_LEVEL=INFO
TRAEFIK_ACCESSLOG_ENABLED=true

# -----------------------------
# PostgreSQL
# -----------------------------
POSTGRES_DB=zitadel
POSTGRES_ADMIN_USER=postgres
POSTGRES_ADMIN_PASSWORD=CHANGE_ME_postgres_admin_password
POSTGRES_ZITADEL_USER=zitadel
POSTGRES_ZITADEL_PASSWORD=CHANGE_ME_zitadel_db_password

# -----------------------------
# Access logs
# -----------------------------
ZITADEL_ACCESS_LOG_STDOUT_ENABLED=true
