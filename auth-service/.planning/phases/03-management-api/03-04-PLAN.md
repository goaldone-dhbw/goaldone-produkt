---
phase: 03-management-api
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified: [
  "src/main/java/de/goaldone/authservice/dto/InvitationRequest.java",
  "src/main/java/de/goaldone/authservice/dto/InvitationResponse.java",
  "src/main/java/de/goaldone/authservice/service/InvitationManagementService.java",
  "src/main/java/de/goaldone/authservice/controller/InvitationManagementController.java",
  "src/test/java/de/goaldone/authservice/controller/InvitationManagementControllerTests.java"
]
autonomous: true
requirements: [F4.1]
must_haves:
  truths:
    - "Management API can create an invitation for a specific email and company"
    - "Invitations have a unique UUID token and an expiry date (default 7 days)"
    - "API prevents creating an invitation if the user is already a member of the organization"
    - "Management API can retrieve an invitation by its token (UUID)"
    - "Management API can cancel/delete an invitation"
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/service/InvitationManagementService.java"
      provides: "Invitation business logic including duplicate checks and expiry"
    - path: "src/main/java/de/goaldone/authservice/controller/InvitationManagementController.java"
      provides: "REST endpoints for invitation management"
  key_links:
    - from: "InvitationManagementService.java"
      to: "MembershipRepository"
      via: "Dependency Injection to check existing memberships"
---

<objective>
Implement the Invitation Management REST API.
This includes logic for generating secure UUID-based invitation tokens, managing their 7-day lifecycle, and enforcing business rules like preventing duplicate invitations for existing members.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-management-api/03-01-SUMMARY.md
@src/main/java/de/goaldone/authservice/domain/Invitation.java
@src/main/java/de/goaldone/authservice/domain/Membership.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invitation Management Service</name>
  <files>
    src/main/java/de/goaldone/authservice/dto/InvitationRequest.java,
    src/main/java/de/goaldone/authservice/dto/InvitationResponse.java,
    src/main/java/de/goaldone/authservice/service/InvitationManagementService.java
  </files>
  <action>
    1. Create `InvitationRequest` and `InvitationResponse` DTOs.
    2. Implement `InvitationManagementService`:
       - `createInvitation(InvitationRequest)`:
         - Validate email and companyId.
         - Check if user with this email is already a member of the company (D-14).
         - Generate UUID token.
         - Set expiry to 7 days from now (D-13).
         - Save and return.
       - `getInvitationByToken(UUID)`: Find invitation.
       - `cancelInvitation(UUID)`: Delete invitation.
  </action>
  <verify>
    <automated>
      ./mvnw compile
    </automated>
  </verify>
  <done>Invitation business logic is implemented and duplicate checks are enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Invitation Management Controller and Integration Tests</name>
  <files>
    src/main/java/de/goaldone/authservice/controller/InvitationManagementController.java,
    src/test/java/de/goaldone/authservice/controller/InvitationManagementControllerTests.java
  </files>
  <action>
    1. Implement `InvitationManagementController` at `/api/v1/invitations`:
       - POST `/`: Create invitation.
       - GET `/{token}`: Get invitation by token.
       - DELETE `/{token}`: Cancel invitation.
    2. Add `InvitationManagementControllerTests`:
       - Test successful invitation creation.
       - Test duplicate invitation prevention (should return 400 Bad Request / ProblemDetail).
       - Test unauthorized access.
  </action>
  <verify>
    <automated>
      ./mvnw test -Dtest=de.goaldone.authservice.controller.InvitationManagementControllerTests
    </automated>
  </verify>
  <done>Invitation management API is exposed and verified.</done>
</task>

</tasks>

<verification>
Verify invitation lifecycle by creating one, fetching it, and then cancelling it via API.
</verification>

<success_criteria>
- Invitations can be created with 7-day expiry.
- Duplicate invitations for existing members are blocked.
- Tokens are secure UUIDs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-api/03-04-SUMMARY.md`
</output>
