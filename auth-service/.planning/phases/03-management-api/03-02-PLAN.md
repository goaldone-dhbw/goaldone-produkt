---
phase: 03-management-api
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [
  "src/main/java/de/goaldone/authservice/dto/UserRequest.java",
  "src/main/java/de/goaldone/authservice/dto/UserResponse.java",
  "src/main/java/de/goaldone/authservice/service/UserManagementService.java",
  "src/main/java/de/goaldone/authservice/controller/UserManagementController.java",
  "src/test/java/de/goaldone/authservice/controller/UserManagementControllerTests.java"
]
autonomous: true
requirements: [F2.1, F2.2, F2.3]
must_haves:
  truths:
    - "Management API can create a new user with multiple emails"
    - "Management API can retrieve a user by ID"
    - "Management API can retrieve a user by their primary email"
    - "Management API can update user status and super_admin flag"
    - "Management API can delete a user and their associated data"
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/service/UserManagementService.java"
      provides: "User management business logic"
    - path: "src/main/java/de/goaldone/authservice/controller/UserManagementController.java"
      provides: "REST endpoints for user management"
  key_links:
    - from: "UserManagementController.java"
      to: "UserManagementService.java"
      via: "Dependency Injection"
---

<objective>
Implement the User Management REST API.
This includes DTOs, a dedicated service for managing users, and a controller providing CRUD operations at /api/v1/users.
Support strict lookup by ID or Primary Email.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-management-api/03-01-SUMMARY.md
@src/main/java/de/goaldone/authservice/domain/User.java
@src/main/java/de/goaldone/authservice/domain/UserEmail.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: User Management Service and DTOs</name>
  <files>
    src/main/java/de/goaldone/authservice/dto/UserRequest.java,
    src/main/java/de/goaldone/authservice/dto/UserResponse.java,
    src/main/java/de/goaldone/authservice/service/UserManagementService.java
  </files>
  <action>
    1. Create `UserRequest` and `UserResponse` DTOs using Lombok `@Data` / `@Builder`.
       - Include fields for password, status, superAdmin, and list of emails.
    2. Implement `UserManagementService`:
       - `createUser(UserRequest)`: Create user, hash password, add emails.
       - `getUserById(UUID)`: Find user or throw `EntityNotFoundException`.
       - `getUserByPrimaryEmail(String)`: Find user via `UserRepository` and `UserEmailRepository`.
       - `updateUser(UUID, UserRequest)`: Update status, superAdmin, etc.
       - `deleteUser(UUID)`: Remove user and related entities.
  </action>
  <verify>
    <automated>
      ./mvnw compile
    </automated>
  </verify>
  <done>User management business logic is implemented and types are defined.</done>
</task>

<task type="auto">
  <name>Task 2: User Management Controller and Integration Tests</name>
  <files>
    src/main/java/de/goaldone/authservice/controller/UserManagementController.java,
    src/test/java/de/goaldone/authservice/controller/UserManagementControllerTests.java
  </files>
  <action>
    1. Implement `UserManagementController` at `/api/v1/users`:
       - POST `/`: Create user.
       - GET `/{id}`: Get user by ID.
       - GET `/search?email={email}`: Get user by primary email (Strict lookup).
       - PUT `/{id}`: Update user.
       - DELETE `/{id}`: Delete user.
    2. Add `UserManagementControllerTests` using `@SpringBootTest` and `MockMvc`:
       - Test with valid M2M token (can be mocked or obtained from auth server).
       - Verify CRUD operations and RFC 7807 error responses for 404/400 cases.
  </action>
  <verify>
    <automated>
      ./mvnw test -Dtest=de.goaldone.authservice.controller.UserManagementControllerTests
    </automated>
  </verify>
  <done>User management API is exposed and verified with tests.</done>
</task>

</tasks>

<verification>
Check /api/v1/users endpoints using curl with a Bearer token from mgmt-client.
</verification>

<success_criteria>
- Users can be created, read, updated, and deleted via API.
- Lookup by primary email works.
- Authentication/Authorization is enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-api/03-02-SUMMARY.md`
</output>
