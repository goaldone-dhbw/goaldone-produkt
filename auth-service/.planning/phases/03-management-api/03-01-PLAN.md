---
phase: 03-management-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/main/java/de/goaldone/authservice/config/AuthorizationServerConfig.java",
  "src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java",
  "src/main/java/de/goaldone/authservice/config/TokenCustomizerConfig.java",
  "src/main/java/de/goaldone/authservice/domain/Invitation.java",
  "src/main/java/de/goaldone/authservice/repository/InvitationRepository.java",
  "src/main/resources/db/changelog/03-invitation-schema.xml",
  "src/main/resources/db/changelog/db.changelog-master.xml",
  "src/main/java/de/goaldone/authservice/exception/GlobalExceptionHandler.java"
]
autonomous: true
requirements: [F1.6, F7.1, F7.2, F4.1]
must_haves:
  truths:
    - "Management client can obtain an access token using client_credentials grant"
    - "Management access token contains the 'auth-service-mgmt' audience claim"
    - "Endpoints under /api/v1/** require a valid JWT with the correct audience"
    - "Failed API requests return RFC 7807 Problem Details responses"
    - "Database contains an invitations table with UUID id and expiry fields"
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/domain/Invitation.java"
      provides: "Invitation entity model"
    - path: "src/main/resources/db/changelog/03-invitation-schema.xml"
      provides: "Database schema for invitations"
    - path: "src/main/java/de/goaldone/authservice/exception/GlobalExceptionHandler.java"
      provides: "RFC 7807 error handling"
  key_links:
    - from: "AuthorizationServerConfig.java"
      to: "mgmt-client"
      via: "RegisteredClient definition"
    - from: "DefaultSecurityConfig.java"
      to: "/api/v1/**"
      via: "securityMatcher and oauth2ResourceServer"
---

<objective>
Setup the security infrastructure and foundational components for the Management API.
This includes configuring the Authorization Server to support a Management Client (M2M),
enabling Resource Server capabilities for the API path, and establishing the database schema for Invitations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-management-api/03-CONTEXT.md
@src/main/java/de/goaldone/authservice/config/AuthorizationServerConfig.java
@src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java
@src/main/java/de/goaldone/authservice/config/TokenCustomizerConfig.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Management Security (M2M)</name>
  <files>
    src/main/java/de/goaldone/authservice/config/AuthorizationServerConfig.java,
    src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java,
    src/main/java/de/goaldone/authservice/config/TokenCustomizerConfig.java
  </files>
  <action>
    1. Update `AuthorizationServerConfig.registeredClientRepository` to include a new `RegisteredClient` for "mgmt-client":
       - ID: UUID, Client ID: "mgmt-client", Secret: "{noop}mgmt-secret"
       - Grant Type: `CLIENT_CREDENTIALS`
       - Scope: "mgmt:admin"
    2. Update `TokenCustomizerConfig.tokenCustomizer` to add "aud" claim with value "auth-service-mgmt" when the grant type is `client_credentials`.
    3. Update `DefaultSecurityConfig.defaultSecurityFilterChain` to:
       - Add a separate `SecurityFilterChain` with `@Order(1)` that matches `/api/v1/**`.
       - Configure it as an OAuth2 Resource Server with JWT support.
       - Require authenticated requests for this chain.
  </action>
  <verify>
    <automated>
      ./mvnw test -Dtest=de.goaldone.authservice.AuthorizationServerEndpointsTests
    </automated>
  </verify>
  <done>Management client can authenticate and /api/v1/** path is protected by JWT.</done>
</task>

<task type="auto">
  <name>Task 2: Infrastructure: Invitation Schema and Global Error Handling</name>
  <files>
    src/main/java/de/goaldone/authservice/domain/Invitation.java,
    src/main/java/de/goaldone/authservice/repository/InvitationRepository.java,
    src/main/resources/db/changelog/03-invitation-schema.xml,
    src/main/resources/db/changelog/db.changelog-master.xml,
    src/main/java/de/goaldone/authservice/exception/GlobalExceptionHandler.java
  </files>
  <action>
    1. Create `Invitation` entity:
       - Fields: UUID id (primary key), String email, UUID companyId, UUID inviterId, LocalDateTime expiresAt, LocalDateTime createdAt.
       - Relationships: Linked to `Company` via `company_id`.
    2. Create `InvitationRepository` interface extending `JpaRepository`.
    3. Create Liquibase changelog `03-invitation-schema.xml` with `invitations` table and foreign key to `companies`.
    4. Register the new changelog in `db.changelog-master.xml`.
    5. Implement `GlobalExceptionHandler` using `@RestControllerAdvice`:
       - Extend `ResponseEntityExceptionHandler`.
       - Handle common exceptions (EntityNotFound, BadRequest) and return `ProblemDetail` (RFC 7807).
  </action>
  <verify>
    <automated>
      ./mvnw compile
    </automated>
  </verify>
  <done>Database schema updated and RFC 7807 error handling is active.</done>
</task>

</tasks>

<verification>
Validate security by attempting to access a dummy /api/v1 endpoint with and without a valid mgmt-token.
</verification>

<success_criteria>
- mgmt-client can get a token.
- Token has aud: auth-service-mgmt.
- invitations table exists.
- Error responses use ProblemDetail format.
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-api/03-01-SUMMARY.md`
</output>
