---
phase: 03-management-api
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [
  "src/main/java/de/goaldone/authservice/dto/CompanyRequest.java",
  "src/main/java/de/goaldone/authservice/dto/CompanyResponse.java",
  "src/main/java/de/goaldone/authservice/service/OrganizationManagementService.java",
  "src/main/java/de/goaldone/authservice/controller/OrganizationManagementController.java",
  "src/test/java/de/goaldone/authservice/controller/OrganizationManagementControllerTests.java"
]
autonomous: true
requirements: [F3.1, F3.2]
must_haves:
  truths:
    - "Management API can create a new organization with a unique slug"
    - "Management API can retrieve an organization by ID"
    - "Management API can retrieve an organization by its slug"
    - "Management API can update organization name and slug"
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/service/OrganizationManagementService.java"
      provides: "Organization management business logic"
    - path: "src/main/java/de/goaldone/authservice/controller/OrganizationManagementController.java"
      provides: "REST endpoints for organization management"
  key_links:
    - from: "OrganizationManagementController.java"
      to: "OrganizationManagementService.java"
      via: "Dependency Injection"
---

<objective>
Implement the Organization (Company) Management REST API.
This includes DTOs, a dedicated service for managing companies, and a controller providing CRUD operations at /api/v1/organizations.
Support strict lookup by ID or Slug.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-management-api/03-01-SUMMARY.md
@src/main/java/de/goaldone/authservice/domain/Company.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Organization Management Service and DTOs</name>
  <files>
    src/main/java/de/goaldone/authservice/dto/CompanyRequest.java,
    src/main/java/de/goaldone/authservice/dto/CompanyResponse.java,
    src/main/java/de/goaldone/authservice/service/OrganizationManagementService.java
  </files>
  <action>
    1. Create `CompanyRequest` and `CompanyResponse` DTOs using Lombok.
       - Include fields: name, slug.
    2. Implement `OrganizationManagementService`:
       - `createOrganization(CompanyRequest)`: Create new `Company`.
       - `getOrganizationById(UUID)`: Find company or throw `EntityNotFoundException`.
       - `getOrganizationBySlug(String)`: Find company via `CompanyRepository`.
       - `updateOrganization(UUID, CompanyRequest)`: Update name and slug.
  </action>
  <verify>
    <automated>
      ./mvnw compile
    </automated>
  </verify>
  <done>Organization management logic and DTOs are ready.</done>
</task>

<task type="auto">
  <name>Task 2: Organization Management Controller and Integration Tests</name>
  <files>
    src/main/java/de/goaldone/authservice/controller/OrganizationManagementController.java,
    src/test/java/de/goaldone/authservice/controller/OrganizationManagementControllerTests.java
  </files>
  <action>
    1. Implement `OrganizationManagementController` at `/api/v1/organizations`:
       - POST `/`: Create organization.
       - GET `/{id}`: Get organization by ID.
       - GET `/search?slug={slug}`: Get organization by slug.
       - PUT `/{id}`: Update organization.
    2. Add `OrganizationManagementControllerTests`:
       - Test CRUD endpoints with M2M token.
       - Verify unique slug constraint returns 400/409 (ProblemDetail).
  </action>
  <verify>
    <automated>
      ./mvnw test -Dtest=de.goaldone.authservice.controller.OrganizationManagementControllerTests
    </automated>
  </verify>
  <done>Organization management API is exposed and verified.</done>
</task>

</tasks>

<verification>
Check /api/v1/organizations endpoints using curl with mgmt-token.
</verification>

<success_criteria>
- Organizations can be created, read, and updated.
- Lookup by slug works.
- Duplicate slug causes an error response.
</success_criteria>

<output>
After completion, create `.planning/phases/03-management-api/03-03-SUMMARY.md`
</output>
