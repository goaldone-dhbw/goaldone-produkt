---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [pom.xml, src/main/java/de/goaldone/authservice/config/AuthorizationServerConfig.java, src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java, src/test/java/de/goaldone/authservice/AuthorizationServerEndpointsTests.java]
autonomous: true
requirements: [Task 1.1, Task 1.2]

must_haves:
  truths:
    - "OIDC Discovery endpoint returns valid issuer configuration"
    - "JWKS endpoint returns a non-empty list of public keys"
    - "Application logs show 'test-client' is registered"
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/config/AuthorizationServerConfig.java"
      provides: "Auth Server SecurityFilterChain"
    - path: "src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java"
      provides: "Standard SecurityFilterChain"
    - path: "src/test/java/de/goaldone/authservice/AuthorizationServerEndpointsTests.java"
      provides: "Integration tests for OAuth2 endpoints"
  key_links:
    - from: "AuthorizationServerConfig.java"
      to: "/.well-known/openid-configuration"
      via: "Protocol endpoints"
---

<objective>
Setup Spring Authorization Server skeleton and configure JWKS endpoint with a test client.
Purpose: Establish the core OAuth 2.1 authorization server functionality.
Output: Security configuration classes, OIDC/OAuth 2.1 endpoints, and integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/spring-auth-server-research.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Authorization Server Dependencies and Basic Config</name>
  <files>pom.xml, src/main/java/de/goaldone/authservice/config/AuthorizationServerConfig.java, src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java, src/test/java/de/goaldone/authservice/AuthorizationServerEndpointsTests.java</files>
  <action>
    - Add `spring-security-oauth2-authorization-server` dependency to `pom.xml`.
    - Create `AuthorizationServerConfig.java` to define `SecurityFilterChain` for the auth server.
    - Create `DefaultSecurityConfig.java` to define `SecurityFilterChain` for the default security (login page).
    - Enable standard OIDC/OAuth 2.1 settings as per RESEARCH.md.
    - Create `AuthorizationServerEndpointsTests.java` using `@SpringBootTest` and `MockMvc` to verify `/.well-known/openid-configuration`.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=AuthorizationServerEndpointsTests</automated>
  </verify>
  <done>Authorization server endpoints are active and discovery is working (verified via tests).</done>
</task>

<task type="auto">
  <name>Task 2: Configure JWKS and Test Client</name>
  <files>src/main/java/de/goaldone/authservice/config/AuthorizationServerConfig.java, src/test/java/de/goaldone/authservice/AuthorizationServerEndpointsTests.java</files>
  <action>
    - Generate a static RSA keypair for JWKS (per D-04 in 1-CONTEXT.md).
    - Register a `RegisteredClient` for testing (client-id: `test-client`, secret: `secret`, scopes: `openid, profile`, redirect-uri: `http://127.0.0.1:8080/login/oauth2/code/test-client`).
    - Expose `JWKSource` and `AuthorizationServerSettings` beans.
    - Add a test case to `AuthorizationServerEndpointsTests.java` to verify `/oauth2/jwks` returns keys.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=AuthorizationServerEndpointsTests</automated>
  </verify>
  <done>JWKS endpoint returns keys and test client is registered (verified via tests).</done>
</task>

</tasks>

<success_criteria>
- OIDC Discovery endpoint is reachable.
- JWKS endpoint returns a valid public key.
- A test client is configured for upcoming validation phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
