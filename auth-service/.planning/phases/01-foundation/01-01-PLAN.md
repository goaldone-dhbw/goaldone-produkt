---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/main/resources/application.yaml, src/main/resources/application-local.yaml, src/main/resources/application-prod.yaml, .env-example, docker-compose.yaml, src/main/resources/db/changelog/db.changelog-master.xml, src/main/resources/db/changelog/01-initial-schema.xml]
autonomous: true
requirements: [Task 1.3]
user_setup:
  - service: database
    why: "PostgreSQL needed for production profile"
    env_vars:
      - name: DB_URL
        source: "PostgreSQL Connection String"
      - name: DB_USERNAME
        source: "DB Admin"
      - name: DB_PASSWORD
        source: "DB Password"

must_haves:
  truths:
    - "Application configuration is valid for local profile"
    - "Liquibase successfully executes schema migrations"
    - "Tables 'users', 'user_emails', 'companies', 'memberships' exist in DB"
    - "Docker Compose can start the PostgreSQL service"
  artifacts:
    - path: "src/main/resources/application.yaml"
      provides: "Base configuration"
    - path: "src/main/resources/db/changelog/01-initial-schema.xml"
      provides: "Initial schema definition"
    - path: "docker-compose.yaml"
      provides: "Infrastructure for production profile"
  key_links:
    - from: "application.yaml"
      to: "db.changelog-master.xml"
      via: "spring.liquibase.change-log"
---

<objective>
Setup the project's configuration foundation, implement the initial database schema using Liquibase, and provide Docker infrastructure for PostgreSQL.
Purpose: Establish the tiered configuration, the multi-email/multi-org data model, and local containerized infrastructure.
Output: Tiered configuration files, .env-example, Liquibase migrations, and docker-compose.yaml.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tiered Configuration and Docker Setup</name>
  <files>src/main/resources/application.yaml, src/main/resources/application-local.yaml, src/main/resources/application-prod.yaml, .env-example, docker-compose.yaml</files>
  <action>
    - Create `application.yaml` as the base config with placeholders (e.g., `${DB_URL}`).
    - Create `application-local.yaml` configured for H2 with `MODE=PostgreSQL`.
    - Create `application-prod.yaml` configured for PostgreSQL (connects to `auth-db` service).
    - Create `.env-example` with all necessary environment variables.
    - Create `docker-compose.yaml` with a `postgresql:16-alpine` service named `auth-db`.
    - Reference D-01 in 1-CONTEXT.md for tiered config requirement.
  </action>
  <verify>
    <automated>docker compose config && ./mvnw spring-boot:help</automated>
  </verify>
  <done>Configuration files and docker-compose.yaml created. Validation of config and docker structure passes.</done>
</task>

<task type="auto">
  <name>Task 2: Initial Database Schema (Liquibase)</name>
  <files>src/main/resources/db/changelog/db.changelog-master.xml, src/main/resources/db/changelog/01-initial-schema.xml</files>
  <action>
    - Initialize `db.changelog-master.xml`.
    - Create `01-initial-schema.xml` with tables:
        - `users`: id (UUID), password, super_admin (boolean), status, created_at, updated_at.
        - `user_emails`: id (UUID), user_id, email (unique), is_primary (boolean).
        - `companies`: id (UUID), name, created_at.
        - `memberships`: user_id, company_id, role (SUPER_ADMIN, COMPANY_ADMIN, USER).
    - Per D-02, D-03 in 1-CONTEXT.md.
  </action>
  <verify>
    <automated>./mvnw compile liquibase:update -Dliquibase.url=jdbc:h2:mem:testdb -Dliquibase.username=sa -Dliquibase.password=</automated>
  </verify>
  <done>Schema migrated successfully to H2.</done>
</task>

</tasks>

<success_criteria>
- Application configuration is tiered and environment-aware.
- Docker Compose setup provides a PostgreSQL instance for the `prod` profile.
- Database schema supports multi-email and multi-org memberships as specified in CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
