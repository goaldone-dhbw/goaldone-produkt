---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified: [src/main/java/de/goaldone/authservice/domain/User.java, src/main/java/de/goaldone/authservice/domain/UserEmail.java, src/main/java/de/goaldone/authservice/domain/Company.java, src/main/java/de/goaldone/authservice/domain/Membership.java, src/main/java/de/goaldone/authservice/repository/UserRepository.java, src/main/java/de/goaldone/authservice/security/CustomUserDetailsService.java, src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java, src/test/java/de/goaldone/authservice/repository/UserRepositoryTests.java, src/test/java/de/goaldone/authservice/domain/EntityMappingTests.java, src/test/java/de/goaldone/authservice/security/CustomUserDetailsServiceTests.java]
autonomous: true
requirements: [Task 1.4]

must_haves:
  truths:
    - "Users can be retrieved from DB by their secondary email address"
    - "Authentication succeeds when providing a valid secondary email and password"
    - "Authentication fails for non-existent emails or invalid passwords"
    - "JPA entities exist for Company and Membership to support multi-org"
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/repository/UserRepository.java"
      provides: "Multi-email lookup query"
    - path: "src/main/java/de/goaldone/authservice/security/CustomUserDetailsService.java"
      provides: "Security identity bridge"
    - path: "src/main/java/de/goaldone/authservice/domain/Company.java"
      provides: "Organization entity"
    - path: "src/main/java/de/goaldone/authservice/domain/Membership.java"
      provides: "User-Organization link with roles"
  key_links:
    - from: "CustomUserDetailsService.java"
      to: "UserRepository.java"
      via: "Dependency injection"
---

<objective>
Implement JPA entities (User, Email, Company, Membership) and a custom UserDetailsService supporting multi-email lookup.
Purpose: Enable users to authenticate using any of their verified email addresses and prepare for multi-org role management.
Output: JPA entities, Repository with custom query, and UserDetailsService implementation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement JPA Entities and Repository</name>
  <files>src/main/java/de/goaldone/authservice/domain/User.java, src/main/java/de/goaldone/authservice/domain/UserEmail.java, src/main/java/de/goaldone/authservice/domain/Company.java, src/main/java/de/goaldone/authservice/domain/Membership.java, src/main/java/de/goaldone/authservice/repository/UserRepository.java, src/test/java/de/goaldone/authservice/repository/UserRepositoryTests.java, src/test/java/de/goaldone/authservice/domain/EntityMappingTests.java</files>
  <action>
    - Create `User` and `UserEmail` entities matching the Liquibase schema (per D-02).
    - Create `Company` and `Membership` entities matching the Liquibase schema (per D-03).
    - `Membership` should use a composite key or appropriate JPA mapping for `user_id` and `company_id` and include the `role` enum.
    - Implement `UserRepository` with a custom query to find a `User` by an email address found in the `UserEmail` table.
    - Ensure appropriate `@OneToMany` relationships (User -> Emails, User -> Memberships).
    - Create `UserRepositoryTests.java` and `EntityMappingTests.java` to verify the logic.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=UserRepositoryTests,EntityMappingTests</automated>
  </verify>
  <done>Entities and multi-email lookup query are implemented and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Custom UserDetailsService</name>
  <files>src/main/java/de/goaldone/authservice/security/CustomUserDetailsService.java, src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java, src/test/java/de/goaldone/authservice/security/CustomUserDetailsServiceTests.java</files>
  <behavior>
    - Given an email address, the service should return the associated User.
    - If the email is not found in the user_emails table, it should throw UsernameNotFoundException.
    - The returned UserDetails should include the user's password and status-based flags.
  </behavior>
  <action>
    - Create `CustomUserDetailsService` implementing `UserDetailsService`.
    - Use `UserRepository` to perform the lookup.
    - Map the domain `User` to a Spring Security `UserDetails` object.
    - Register the `CustomUserDetailsService` bean in `DefaultSecurityConfig.java`.
    - Create `CustomUserDetailsServiceTests.java` to verify the service.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=CustomUserDetailsServiceTests</automated>
  </verify>
  <done>UserDetailsService supports multi-email authentication.</done>
</task>

</tasks>

<success_criteria>
- Users can be retrieved by any of their associated emails.
- Spring Security uses the custom UserDetailsService for authentication.
- Domain model fully represents the multi-org requirements from CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
