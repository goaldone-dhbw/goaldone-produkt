---
phase: 07.1-fix-all-existing-tests
plan: 1
type: execute
wave: 1
depends_on: [07-02]
files_modified: [
  src/main/resources/templates/auth/invitation-landing.html,
  src/main/java/de/goaldone/authservice/controller/InvitationPageController.java,
  src/test/java/de/goaldone/authservice/integration/invitation/InvitationFlowIntegrationTest.java,
  src/test/java/de/goaldone/authservice/controller/InvitationFlowIntegrationTest.java,
  src/test/java/de/goaldone/authservice/service/EmailTemplateRenderingTest.java
]
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "All 96 tests pass without errors or failures"
    - "Thymeleaf template expressions resolve correctly without parse exceptions"
    - "Spring ApplicationContext loads without threshold-exceeded errors"
    - "Integration tests validate OAuth, invitation, user/org/password/account/constraint flows"
    - "Template can conditionally render based on emailMatch boolean variable"
  artifacts:
    - path: "src/main/resources/templates/auth/invitation-landing.html"
      provides: "Invitation landing page with corrected Thymeleaf expressions"
      contains: "th:if"
    - path: "src/main/java/de/goaldone/authservice/controller/InvitationPageController.java"
      provides: "Controller that populates emailMatch model attribute"
      exports: ["landingPage()"]
    - path: "src/test/java/de/goaldone/authservice/integration/invitation/InvitationFlowIntegrationTest.java"
      provides: "Integration tests for invitation flow"
      min_lines: 50
    - path: "src/test/java/de/goaldone/authservice/service/EmailTemplateRenderingTest.java"
      provides: "Unit tests for email template rendering"
      min_lines: 30
  key_links:
    - from: "InvitationPageController.landingPage()"
      to: "Model.addAttribute(emailMatch)"
      via: "model.addAttribute(emailMatch, flowRoute.getEmailMatch().isFound())"
      pattern: "addAttribute.*emailMatch"
    - from: "invitation-landing.html th:if expressions"
      to: "emailMatch boolean value from controller"
      via: "Thymeleaf template evaluation"
      pattern: "th:if=.*emailMatch"
---

<objective>
Fix all existing test failures in phase 7 integration test suite by resolving Thymeleaf template expression issues and ensuring Spring ApplicationContext loads correctly.

**Purpose:** Phase 7 delivered 96 tests, but context-loading and template-parsing exceptions prevent test suite from running. Identified root cause: Thymeleaf template variable `emailMatch` handling in invitation-landing.html conflicts with template expression semantics.

**Output:** 
- Fixed Thymeleaf template expressions (lines 128, 175)
- Verified all 96 tests pass
- Integration test suite ready for continuous use
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/STATE.md
</execution_context>

<context>
## Test Failure Context

From previous execution (Phase 7, Plan 2):
- 96 tests compiled successfully
- 25 errors during test run (0 failures)
- Primary errors: ApplicationContext loading failures in:
  - InvitationFlowIntegrationTest (12 tests)
  - EmailTemplateRenderingTest (11 tests)
- Root cause indicator: Thymeleaf expression parsing error at invitation-landing.html line 175

## Current Template State

File: `src/main/resources/templates/auth/invitation-landing.html`

Lines 128 & 175 use Thymeleaf conditional expressions:
```html
<!-- Line 128: emailMatch case (email found) -->
<div th:if="${emailMatch}">

<!-- Line 175: !emailMatch case (email not found) -->
<div th:if="${!emailMatch}">
```

Issue: Controller passes `emailMatch` as **boolean** (line 76 of InvitationPageController):
```java
model.addAttribute("emailMatch", flowRoute.getEmailMatch().isFound());
```

However, the DTO structure suggests `emailMatch` is an object (InvitationFlowRoute.EmailMatch), not a boolean.

## What Needs Verification

1. Model attribute type: Is `emailMatch` passed as boolean or object?
2. Template expression semantics: Does `!emailMatch` evaluate correctly?
3. Spring initialization: Can context load without template parse errors?
4. Test isolation: Does test database state persist across test methods?
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and Fix Thymeleaf Template Expression Issue</name>
  <files>
    src/main/resources/templates/auth/invitation-landing.html,
    src/main/java/de/goaldone/authservice/controller/InvitationPageController.java
  </files>
  <action>
1. Verify the data type passed to model for `emailMatch`:
   - Read InvitationPageController.landingPage() line 76
   - Confirm whether controller passes boolean (`isFound()`) or object

2. If `emailMatch` is passed as a boolean:
   - Template expressions at lines 128 and 175 are correct
   - But test failures suggest Thymeleaf parser may be choking on negation operator
   - Verify Spring/Thymeleaf version compatibility for `!` operator in th:if expressions
   - If incompatible, change `th:if="${!emailMatch}"` to `th:if="${emailMatch} == false"` or add explicit boolean check

3. If `emailMatch` is passed as an object (InvitationFlowRoute.EmailMatch):
   - Lines 128/175 are incorrect and must reference `.isFound()` method
   - Change line 128 from `th:if="${emailMatch}"` to `th:if="${emailMatch.found}"`
   - Change line 175 from `th:if="${!emailMatch}"` to `th:if="${!emailMatch.found}"`

4. Ensure all references to emailMatch in the template match the actual data type:
   - Search template for all `${emailMatch}` references
   - Verify they either all use boolean checks or all use `.found` property accessor

5. Run test compilation to verify no new template parse errors:
   - `./mvnw clean test-compile`
   - If any template parsing errors occur, iteratively fix expressions until compilation succeeds

Reference: This fixes the root cause of ApplicationContext initialization failures in test suite.
  </action>
  <verify>
    <automated>./mvnw clean test-compile 2>&1 | grep -i "template.*error\|parsing.*error" | wc -l</automated>
  </verify>
  <done>Template compiles without parse errors. No Thymeleaf expression syntax errors reported by Spring at startup.</done>
</task>

<task type="auto">
  <name>Task 2: Run Integration Tests and Fix ApplicationContext Loading Issues</name>
  <files>
    src/test/java/de/goaldone/authservice/integration/invitation/InvitationFlowIntegrationTest.java,
    src/test/java/de/goaldone/authservice/controller/InvitationFlowIntegrationTest.java,
    src/test/java/de/goaldone/authservice/service/EmailTemplateRenderingTest.java
  </files>
  <action>
1. Run the full test suite to identify any remaining ApplicationContext loading issues:
   - `./mvnw clean test 2>&1 | tee test-output.log`
   - Capture full output including any context initialization errors

2. If InvitationFlowIntegrationTest still fails:
   - Check if test class can instantiate application context
   - Verify @SpringBootTest annotation and test profiles are correct
   - If context fails, examine startup errors in test output
   - Common issues: missing beans, property source configuration, template parsing still broken
   - Fix any identified issues in test configuration or beans

3. If EmailTemplateRenderingTest still fails:
   - Check if email template resolution is working
   - Verify Thymeleaf context is properly initialized for template rendering
   - Check for missing model attributes required by templates
   - If templates can't find required attributes, add fallback handling or fix attribute setup

4. For any test that fails due to missing model attributes:
   - Add defensive null checks or default values to controller
   - OR add test fixtures to set required attributes before template rendering

5. Monitor test output for:
   - "ApplicationContext loading failed" → indicates context initialization issue
   - "Failed to instantiate" → indicates missing/misconfigured bean
   - "Template parsing error" → indicates remaining Thymeleaf syntax issues
   - "Assertion failed" → indicates test logic issue (not context issue)

6. Re-run tests in waves:
   - First: `./mvnw test -Dtest=IntegrationTestInfrastructureTest` (validates test framework itself)
   - Second: `./mvnw test -Dtest=InvitationFlowIntegrationTest` (2 test classes)
   - Third: `./mvnw test -Dtest=EmailTemplateRenderingTest` (email rendering)
   - Fourth: Run remaining integration tests
   - Fifth: Full suite

Reference: Template fix from Task 1 should resolve majority of context loading errors.
  </action>
  <verify>
    <automated>./mvnw clean test 2>&1 | tail -50 | grep -E "Tests run:|Errors:|Failures:" | head -1</automated>
  </verify>
  <done>All 96 tests execute without ApplicationContext initialization errors. Test run reports final summary with 0 errors, 0 failures.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Full Test Suite Passes and Document Results</name>
  <files>
    src/test/java/de/goaldone/authservice/integration/oauth/OAuthTokenIntegrationTest.java,
    src/test/java/de/goaldone/authservice/integration/oauth/OIDCDiscoveryIntegrationTest.java,
    src/test/java/de/goaldone/authservice/integration/user/UserManagementIntegrationTest.java,
    src/test/java/de/goaldone/authservice/integration/user/OrganizationManagementIntegrationTest.java,
    src/test/java/de/goaldone/authservice/integration/invitation/InvitationFlowIntegrationTest.java,
    src/test/java/de/goaldone/authservice/integration/password/PasswordResetIntegrationTest.java,
    src/test/java/de/goaldone/authservice/integration/account/AccountLinkingIntegrationTest.java,
    src/test/java/de/goaldone/authservice/integration/constraint/BusinessConstraintIntegrationTest.java,
    src/test/java/de/goaldone/authservice/service/UserManagementServiceConstraintTest.java,
    src/test/java/de/goaldone/authservice/service/AccountLinkingIntegrationTest.java
  </files>
  <action>
1. Run complete test suite with detailed output:
   - `./mvnw clean test -X 2>&1 | tail -200 > final-test-results.txt`
   - Extract test summary:
     - Total tests run
     - Number of errors (should be 0)
     - Number of failures (should be 0)
     - Number of skipped tests
     - Execution time

2. Validate coverage of all test categories:
   - OAuth/OIDC flows: OAuthTokenIntegrationTest, OIDCDiscoveryIntegrationTest
   - User/org management: UserManagementIntegrationTest, OrganizationManagementIntegrationTest
   - Invitation flows: InvitationFlowIntegrationTest (2 classes)
   - Password reset: PasswordResetIntegrationTest
   - Account linking: AccountLinkingIntegrationTest
   - Business constraints: BusinessConstraintIntegrationTest, UserManagementServiceConstraintTest
   - Verify each test class has green checkmark in output

3. Check for any skipped tests (marked with "S"):
   - If tests are skipped due to missing Docker/containers, note this for Phase 8
   - If tests are skipped due to other reasons, investigate and fix

4. Generate final test report:
   - Extract summary line: "Tests run: X, Errors: Y, Failures: Z, Skipped: W"
   - Expected: X=96, Y=0, Z=0, W=0 (or minimal skips)

5. For any test that still fails:
   - Check if it's a test logic issue (assertion failure) vs context loading issue
   - If assertion failure → fix test expectations or implementation
   - If context issue → revisit Task 2 fixes

Reference: All 96 tests should pass per phase 7.2 plan scope.
  </action>
  <verify>
    <automated>./mvnw clean test 2>&1 | grep "Tests run:"</automated>
  </verify>
  <done>Full test suite passes: all 96 tests execute successfully with 0 errors and 0 failures. Test report confirms all critical flows (OAuth, invitations, user/org management, password reset, account linking, constraints) are covered and passing.</done>
</task>

</tasks>

<verification>
## Phase 7.1 Success Criteria

All tests must pass with zero errors or failures:

1. **Template Parsing**: No Thymeleaf expression syntax errors during Spring initialization
2. **Context Loading**: ApplicationContext initializes successfully for all test classes
3. **Test Execution**: All 96 tests execute without ApplicationContext threshold errors
4. **Coverage**: All critical flows validated:
   - OAuth 2.0 token issuance and validation
   - OIDC discovery endpoint
   - User management CRUD and constraints
   - Organization management CRUD
   - Invitation landing page and acceptance flow
   - Password reset request and flow
   - Account linking eligibility and acceptance
   - Business constraints (last-admin, role validation)
5. **Regression**: No existing functionality broken by template fixes

## Verification Commands

```bash
# Compile and check for template errors
./mvnw clean test-compile

# Run full test suite
./mvnw clean test

# Extract summary
./mvnw clean test 2>&1 | tail -20 | grep -E "Tests run:|Errors:|Failures:"

# Count passing test classes
find src/test -name "*Test.java" | wc -l
```
</verification>

<success_criteria>
- [ ] Template expressions compile without Thymeleaf parse errors
- [ ] All 96 integration and unit tests run to completion
- [ ] Test summary reports: "Tests run: 96, Errors: 0, Failures: 0"
- [ ] No ApplicationContext initialization threshold errors in any test
- [ ] All test categories pass: OAuth, OIDC, User/Org, Invitation, Password Reset, Account Linking, Constraints
- [ ] Git status shows test-related changes (if any) committed cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-fix-all-existing-tests/07.1-01-SUMMARY.md` with:

```markdown
# Phase 7.1 Plan 1 Summary — Fix All Existing Tests

**Completed:** [date]
**Status:** ✅ ALL TESTS PASSING

## Root Cause & Fix

**Root Cause:** Thymeleaf template expression parsing error in invitation-landing.html causing ApplicationContext initialization to fail during test setup.

**Specific Issue:** 
- Template used `th:if="${!emailMatch}"` (line 175) to negate model attribute
- Controller passed boolean value via `model.addAttribute("emailMatch", ...)`
- Thymeleaf parser unable to resolve negation operator in certain Spring Boot/Thymeleaf versions

**Resolution:**
[Describe exact changes made to template and/or controller]

## Test Results

- Total Tests: 96
- Errors: 0
- Failures: 0
- Coverage: OAuth, OIDC, User/Org/Invitation/Password/Account/Constraint flows
- Execution Time: [X seconds]

## Files Fixed

- src/main/resources/templates/auth/invitation-landing.html
- src/main/java/de/goaldone/authservice/controller/InvitationPageController.java

## Verification

✅ Template compiles without parse errors
✅ All integration test classes initialize successfully
✅ All 96 tests execute and pass
✅ No regressions in existing functionality
```
</output>
