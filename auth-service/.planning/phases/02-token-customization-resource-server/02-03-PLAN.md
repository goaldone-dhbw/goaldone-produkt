---
phase: 02-token-customization-resource-server
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "src/main/java/de/goaldone/authservice/config/TokenCustomizerConfig.java",
  "src/main/java/de/goaldone/authservice/security/CustomUserDetailsService.java",
  "src/test/java/de/goaldone/authservice/security/TokenCustomizationTests.java"
]
autonomous: true
requirements: [JWT-01, JWT-02, RS-01, F7.2]

must_haves:
  truths:
    - "JWT Access Token contains 'authorities' claim with ROLE_... and ORG_... strings"
    - "JWT Access Token contains 'emails' claim with only verified emails"
    - "JWT Access Token contains 'primary_email' claim"
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/config/TokenCustomizerConfig.java"
      provides: "OAuth2TokenCustomizer bean"
  key_links:
    - from: "TokenCustomizerConfig"
      to: "UserRepository"
      via: "Fetch verified emails and primary email from database"
    - from: "TokenCustomizerConfig"
      to: "User entity"
      via: "JwtEncodingContext principal"
---

<objective>
Implement JWT token customization to include custom claims required by the Resource Server.
This includes authority mapping (Multi-Org roles) and user email information.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-token-customization-resource-server/02-01-SUMMARY.md
@src/main/java/de/goaldone/authservice/domain/User.java
@src/main/java/de/goaldone/authservice/domain/Membership.java
@src/main/java/de/goaldone/authservice/repository/UserRepository.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CustomUserDetailsService to load roles</name>
  <files>src/main/java/de/goaldone/authservice/security/CustomUserDetailsService.java</files>
  <action>
    - Update `loadUserByUsername` to populate authorities based on user memberships.
    - Authorities should include:
        - `ROLE_SUPER_ADMIN` if the user has a membership with `Role.SUPER_ADMIN`.
        - `ORG_{companyId}_{ROLE}` for all memberships.
    - Ensure `CustomUserDetails` (or similar) is used to hold the full User object if needed by the customizer, or pass necessary info via authorities.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=CustomUserDetailsServiceTests</automated>
  </verify>
  <done>UserDetailsService provides correctly formatted authorities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement OAuth2TokenCustomizer and integration tests</name>
  <files>src/main/java/de/goaldone/authservice/config/TokenCustomizerConfig.java, src/test/java/de/goaldone/authservice/security/TokenCustomizationTests.java</files>
  <behavior>
    - Access tokens should contain 'authorities' claim (List of Strings).
    - Access tokens should contain 'emails' claim (List of verified email strings).
    - Access tokens should contain 'primary_email' claim (String).
    - Claims should be derived from the authenticated principal.
  </behavior>
  <action>
    - Define `OAuth2TokenCustomizer<JwtEncodingContext>` bean in `TokenCustomizerConfig`.
    - Extract authorities from `context.getPrincipal()`.
    - Fetch verified emails and primary email from the User entity (re-load from repository if necessary).
    - Implement `TokenCustomizationTests` using `@SpringBootTest` and performing a token request.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=TokenCustomizationTests</automated>
  </verify>
  <done>JWT tokens are correctly customized and verified via tests.</done>
</task>

</tasks>

<success_criteria>
- Access tokens issued by the server contain all required claims.
- Authorities follow the specified naming convention (`ROLE_...`, `ORG_...`).
- Only verified emails are included in the 'emails' claim.
- All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-token-customization-resource-server/02-03-SUMMARY.md`
</output>
