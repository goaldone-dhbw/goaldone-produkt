---
phase: 02-token-customization-resource-server
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "src/main/java/de/goaldone/authservice/config/SuperAdminProperties.java",
  "src/main/java/de/goaldone/authservice/startup/BootstrapRunner.java",
  "src/main/resources/application.yaml",
  "src/test/java/de/goaldone/authservice/startup/BootstrapRunnerTests.java"
]
autonomous: true
requirements: [SA-01, F3.2]

must_haves:
  truths:
    - "System organization (Super Admin Org) is created on startup if missing"
    - "Super Admin user is created on startup if missing"
    - "Super Admin credentials are taken from environment variables"
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/config/SuperAdminProperties.java"
      provides: "Type-safe configuration for bootstrapping"
    - path: "src/main/java/de/goaldone/authservice/startup/BootstrapRunner.java"
      provides: "Startup logic implementation"
  key_links:
    - from: "BootstrapRunner"
      to: "CompanyRepository"
      via: "Dependency Injection"
    - from: "BootstrapRunner"
      to: "UserRepository"
      via: "Dependency Injection"
---

<objective>
Implement the Super-Admin bootstrapping logic.
The system must ensure that a "Super Admin Organization" and at least one "Super Admin" user exist on startup.
Credentials should be configurable via environment variables (SUPER_ADMIN_EMAIL, SUPER_ADMIN_PASSWORD).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-token-customization-resource-server/02-01-SUMMARY.md
@src/main/java/de/goaldone/authservice/domain/Role.java
@src/main/java/de/goaldone/authservice/domain/UserStatus.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SuperAdminProperties and configure application.yaml</name>
  <files>src/main/java/de/goaldone/authservice/config/SuperAdminProperties.java, src/main/resources/application.yaml</files>
  <action>
    - Create `SuperAdminProperties` class with `@ConfigurationProperties(prefix = "app.super-admin")`.
    - Include fields: `orgName`, `orgSlug`, `email`, `password`.
    - Update `application.yaml` to include default values and map `email`/`password` to ENV vars (`SUPER_ADMIN_EMAIL`, `SUPER_ADMIN_PASSWORD`).
    - Ensure `@EnableConfigurationProperties(SuperAdminProperties.class)` is added to a configuration class (or the main application).
  </action>
  <verify>
    <automated>./mvnw compile</automated>
  </verify>
  <done>Configuration properties defined and mapped to environment variables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BootstrapRunner and integration tests</name>
  <files>src/main/java/de/goaldone/authservice/startup/BootstrapRunner.java, src/test/java/de/goaldone/authservice/startup/BootstrapRunnerTests.java</files>
  <behavior>
    - On startup, check if a company with `orgSlug` exists.
    - If not, create it with `orgName`.
    - Check if a user with `email` exists.
    - If not, create the user (ACTIVE status, BCrypt password).
    - Ensure the user has a `Membership` in the Super Admin Org with `Role.SUPER_ADMIN`.
    - Set the Super Admin's email as `verified = true`.
  </behavior>
  <action>
    - Implement `ApplicationRunner` in `BootstrapRunner`.
    - Use `CompanyRepository`, `UserRepository`, `PasswordEncoder`.
    - Implement tests in `BootstrapRunnerTests` using `@SpringBootTest`.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=BootstrapRunnerTests</automated>
  </verify>
  <done>Bootstrap logic implemented and verified via integration tests.</done>
</task>

</tasks>

<success_criteria>
- Application starts successfully and creates Super Admin data if missing.
- Configuration is correctly picked up from ENV vars.
- Integration tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-token-customization-resource-server/02-02-SUMMARY.md`
</output>
