---
phase: 04-invitation-reset-flows
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: [src/main/java/de/goaldone/authservice/controller/PasswordResetController.java, src/main/resources/templates/auth/forgot-password.html, src/main/resources/templates/auth/reset-password.html, src/test/java/de/goaldone/authservice/controller/PasswordResetTests.java]
autonomous: true
requirements: [F5.1, F5.3, F5.4]

must_haves:
  truths:
    - "Users can request a password reset by providing their email (F5.1)."
    - "System provides enumeration protection by returning a generic message regardless of email existence (D-10, F5.3)."
    - "Users can set a new password via a valid reset token (F5.2)."
    - "Upon successful password reset, all other active sessions for the user are invalidated (D-06, F5.4)."
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/controller/PasswordResetController.java"
      provides: "Password reset endpoints"
    - path: "src/main/resources/templates/auth/forgot-password.html"
      provides: "Reset request UI"
  key_links:
    - from: "src/main/java/de/goaldone/authservice/controller/PasswordResetController.java"
      to: "SessionRegistry"
      via: "Session invalidation"
---

<objective>
Implement the password recovery flow with high security standards, including enumeration protection and strict session invalidation.

Purpose: To allow users to regain access to their accounts securely while protecting against automated probing.
Output: Complete password reset flow with session management.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@4-CONTEXT.md
@.planning/phases/04-invitation-reset-flows/RESEARCH.md
@.planning/phases/04-invitation-reset-flows/04-01-SUMMARY.md
@.planning/phases/04-invitation-reset-flows/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forgot Password Request and Enumeration Protection</name>
  <files>src/main/java/de/goaldone/authservice/controller/PasswordResetController.java, src/main/resources/templates/auth/forgot-password.html, src/test/java/de/goaldone/authservice/controller/PasswordResetTests.java</files>
  <behavior>
    - GET /forgot-password shows the request form.
    - POST /forgot-password accepts an email.
    - If user exists, a PASSWORD_RESET token is generated and emailed (per F5.1).
    - Regardless of user existence, the response is a generic success message (per D-10, F5.3).
  </behavior>
  <action>
    - Create `PasswordResetController`.
    - Implement `forgotPassword` GET/POST endpoints.
    - Create `forgot-password.html` using base layout.
    - Integrate with `VerificationTokenService` and `MailService`.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=PasswordResetTests#testEnumerationProtection</automated>
  </verify>
  <done>Password reset request flow is secure against user enumeration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Password Reset Completion and Session Invalidation</name>
  <files>src/main/java/de/goaldone/authservice/controller/PasswordResetController.java, src/main/resources/templates/auth/reset-password.html, src/test/java/de/goaldone/authservice/controller/PasswordResetTests.java</files>
  <behavior>
    - GET /reset-password?token={token} validates the token.
    - POST /reset-password accepts new password and confirms token.
    - Successful reset updates user password in database.
    - After reset, all active sessions for the user are invalidated using SessionRegistry (per D-06, F5.4).
  </behavior>
  <action>
    - Implement `resetPassword` GET/POST endpoints.
    - Create `reset-password.html`.
    - Use `SessionRegistry` (from Plan 04-01) to find sessions by username and expire them.
    - Token is consumed (deleted) after use.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=PasswordResetTests#testSessionInvalidationAfterReset</automated>
  </verify>
  <done>Password reset completion securely updates credentials and terminates existing sessions.</done>
</task>

</tasks>

<verification>
- Password reset request handles enumeration protection.
- Reset token validation works as expected.
- Sessions are invalidated after password change.
</verification>

<success_criteria>
- PasswordResetTests pass.
- Sessions for the user are cleared in database after reset.
- VerificationToken is consumed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-invitation-reset-flows/04-04-SUMMARY.md`
</output>
