---
phase: 04-invitation-reset-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pom.xml, src/main/java/de/goaldone/authservice/domain/VerificationToken.java, src/main/java/de/goaldone/authservice/domain/TokenType.java, src/main/java/de/goaldone/authservice/repository/VerificationTokenRepository.java, src/main/java/de/goaldone/authservice/service/VerificationTokenService.java, src/main/java/de/goaldone/authservice/config/SessionConfig.java, src/main/resources/db/changelog/04-security-foundation.xml, src/test/java/de/goaldone/authservice/service/VerificationTokenServiceTests.java, src/test/java/de/goaldone/authservice/security/SessionSecurityTests.java]
autonomous: true
requirements: [F4.1, F5.2]

must_haves:
  truths:
    - "System can generate 32+ character secure random tokens for invitations and password resets (D-07)."
    - "Tokens are stored in a generic VerificationToken entity with an expiry date (D-08)."
    - "Active sessions are stored in the database via Spring Session JDBC (D-05)."
    - "SessionRegistry is available for finding and invalidating user sessions (D-06)."
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/domain/VerificationToken.java"
      provides: "VerificationToken entity"
    - path: "src/main/java/de/goaldone/authservice/config/SessionConfig.java"
      provides: "Spring Session JDBC and SessionRegistry configuration"
  key_links:
    - from: "src/main/java/de/goaldone/authservice/config/SessionConfig.java"
      to: "findByIndexNameSessionRepository"
      via: "Dependency Injection"
---

<objective>
Setup the security infrastructure required for Invitation and Password Reset flows. This includes the generic verification token system and persistent, cluster-safe session management.

Purpose: To provide secure, time-limited credentials for user flows and enable administrative session control.
Output: Verification token system and database-backed sessions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@4-CONTEXT.md
@.planning/phases/04-invitation-reset-flows/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Verification Token System</name>
  <files>src/main/java/de/goaldone/authservice/domain/VerificationToken.java, src/main/java/de/goaldone/authservice/domain/TokenType.java, src/main/java/de/goaldone/authservice/repository/VerificationTokenRepository.java, src/main/java/de/goaldone/authservice/service/VerificationTokenService.java, src/main/resources/db/changelog/04-security-foundation.xml, src/test/java/de/goaldone/authservice/service/VerificationTokenServiceTests.java</files>
  <behavior>
    - Token generation produces a cryptographically secure string of at least 32 characters (per D-07).
    - Tokens can be created for types INVITATION and PASSWORD_RESET (per D-08).
    - VerificationToken stores email and an expiry date.
    - Service can validate a token string and return the associated email/target if not expired.
  </behavior>
  <action>
    - Create `TokenType` enum: `INVITATION`, `PASSWORD_RESET`.
    - Create `VerificationToken` entity with fields: `id` (UUID), `token` (String, unique), `type` (TokenType), `email` (String), `expiryDate` (LocalDateTime).
    - Implement `VerificationTokenRepository`.
    - Implement `VerificationTokenService` with methods: `createToken(String email, TokenType type)`, `verifyToken(String token, TokenType type)`.
    - Use `SecureRandom` or `String.valueOf(UUID.randomUUID())` + more randomness to ensure 32+ chars.
    - Add Liquibase migration `04-verification-tokens.xml` (referenced in master) to create the `verification_tokens` table.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=VerificationTokenServiceTests</automated>
  </verify>
  <done>Verification token system is operational with unit tests and database schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure Spring Session JDBC</name>
  <files>pom.xml, src/main/java/de/goaldone/authservice/config/SessionConfig.java, src/main/resources/db/changelog/04-security-foundation.xml, src/test/java/de/goaldone/authservice/security/SessionSecurityTests.java</files>
  <behavior>
    - Adding spring-session-jdbc dependency enables database-backed sessions (per D-05).
    - Sessions are persisted to the database and can be retrieved by principal name.
    - SessionRegistry is correctly exposed for session invalidation (per D-06).
  </behavior>
  <action>
    - Add `spring-session-jdbc` dependency to `pom.xml`.
    - Create `SessionConfig.java` with `@EnableJdbcHttpSession`.
    - Expose `SessionRegistry` bean using `SpringSessionBackedSessionRegistry`.
    - Add Liquibase migration for `SPRING_SESSION` and `SPRING_SESSION_ATTRIBUTES` tables (refer to Spring Session JDBC documentation for schema).
    - Update `application-local.yaml` to set `spring.session.jdbc.initialize-schema=always` for H2 compatibility.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=SessionSecurityTests</automated>
  </verify>
  <done>Sessions are stored in the database and SessionRegistry is accessible.</done>
</task>

</tasks>

<verification>
- VerificationToken system implemented and tested.
- Spring Session JDBC configured and table schema created.
- SessionRegistry available in context.
</verification>

<success_criteria>
- VerificationTokenServiceTests pass.
- SessionSecurityTests pass (verifying session persistence).
- Liquibase migrations successfully applied.
</success_criteria>

<output>
After completion, create `.planning/phases/04-invitation-reset-flows/04-01-SUMMARY.md`
</output>
