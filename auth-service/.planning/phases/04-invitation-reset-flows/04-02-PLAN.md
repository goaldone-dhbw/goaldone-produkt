---
phase: 04-invitation-reset-flows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/main/java/de/goaldone/authservice/service/MailService.java, src/main/java/de/goaldone/authservice/service/LocalMailService.java, src/main/java/de/goaldone/authservice/service/SmtpMailService.java, src/main/java/de/goaldone/authservice/config/MailConfig.java, src/main/resources/static/css/base.css, src/main/resources/templates/fragments/layout.html, src/main/resources/templates/mail/invitation.html, src/main/resources/templates/mail/password-reset.html, src/test/java/de/goaldone/authservice/service/MailServiceTests.java]
autonomous: true
requirements: [F5.1]

must_haves:
  truths:
    - "System provides a MailService that logs to console in 'local' profile and uses SMTP in 'prod' (D-03)."
    - "Email templates are defined using Thymeleaf for invitation and password reset flows (D-04)."
    - "Global styling uses GoaldoneTheme palette with vanilla CSS variables (D-01, D-02)."
    - "A base Thymeleaf layout exists for consistent UI across all auth pages."
  artifacts:
    - path: "src/main/resources/static/css/base.css"
      provides: "GoaldoneTheme CSS variables"
    - path: "src/main/java/de/goaldone/authservice/service/MailService.java"
      provides: "Email delivery interface"
  key_links:
    - from: "src/main/java/de/goaldone/authservice/config/MailConfig.java"
      to: "MailService"
      via: "@Bean with @Profile"
---

<objective>
Establish the visual identity and communication infrastructure for user flows. This includes the theme-compliant CSS and the conditional mail delivery service.

Purpose: To ensure consistent branding and reliable delivery of time-sensitive invitation and reset links.
Output: Mail delivery system and base UI styling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@4-CONTEXT.md
@.planning/phases/04-invitation-reset-flows/RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Conditional Mail Service</name>
  <files>src/main/java/de/goaldone/authservice/service/MailService.java, src/main/java/de/goaldone/authservice/service/LocalMailService.java, src/main/java/de/goaldone/authservice/service/SmtpMailService.java, src/main/java/de/goaldone/authservice/config/MailConfig.java, src/main/resources/templates/mail/invitation.html, src/main/resources/templates/mail/password-reset.html, src/test/java/de/goaldone/authservice/service/MailServiceTests.java</files>
  <behavior>
    - MailService interface supports sending invitations and password reset emails.
    - LocalMailService (local profile) logs email content to the console (per D-03).
    - SmtpMailService (prod profile) uses JavaMailSender.
    - Thymeleaf is used for email HTML content (per D-04).
  </behavior>
  <action>
    - Create `MailService` interface.
    - Implement `LocalMailService` with SLF4J logging of recipient, subject, and body.
    - Implement `SmtpMailService` using `JavaMailSender` and `MimeMessageHelper`.
    - Create `MailConfig` with `@Profile` gated `@Bean` definitions.
    - Create basic Thymeleaf templates in `src/main/resources/templates/mail/`.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=MailServiceTests</automated>
  </verify>
  <done>Mail service is functional with both local and production implementations.</done>
</task>

<task type="auto">
  <name>Task 2: Establish Visual Identity and Layout</name>
  <files>src/main/resources/static/css/base.css, src/main/resources/templates/fragments/layout.html</files>
  <action>
    - Create `src/main/resources/static/css/base.css` with CSS variables for GoaldoneTheme:
      - `--primary-500: #63729c;`
      - `--surface-500: #556daa;`
      - `--accent-500: #a85791;`
      - And other standard variables (spacing, fonts).
    - Use vanilla CSS (no frameworks) for layout basics (D-02).
    - Create `src/main/resources/templates/fragments/layout.html` with `<head>`, `<header>`, `<footer>`, and a `layout:fragment="content"` placeholder.
  </action>
  <verify>
    <automated>ls src/main/resources/static/css/base.css src/main/resources/templates/fragments/layout.html</automated>
  </verify>
  <done>The visual identity foundation is laid with theme variables and base layout.</done>
</task>

</tasks>

<verification>
- MailService implementations tested and conditional loading verified.
- Base CSS contains correct theme hex codes.
- Layout fragment exists and is ready for use in views.
</verification>

<success_criteria>
- MailServiceTests pass.
- CSS file exists with required variables.
- Layout template exists.
</success_criteria>

<output>
After completion, create `.planning/phases/04-invitation-reset-flows/04-02-SUMMARY.md`
</output>
