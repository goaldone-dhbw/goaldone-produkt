---
phase: 04-invitation-reset-flows
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: [src/main/java/de/goaldone/authservice/controller/InvitationController.java, src/main/java/de/goaldone/authservice/service/InvitationManagementService.java, src/main/resources/templates/auth/invitation-landing.html, src/main/resources/templates/auth/invitation-set-password.html, src/test/java/de/goaldone/authservice/controller/InvitationFlowTests.java]
autonomous: true
requirements: [F4.2, F4.3, F4.4]

must_haves:
  truths:
    - "Invited users can access a landing page via their tokenized invitation link (F4.2)."
    - "Logged-out users who already have an account are prompted to log in to accept the invitation (D-09)."
    - "Authenticated users can accept invitations directly with one click (F4.4)."
    - "New users (INVITED status) can set a password to activate their account (F4.3)."
  artifacts:
    - path: "src/main/java/de/goaldone/authservice/controller/InvitationController.java"
      provides: "Invitation flow endpoints"
    - path: "src/main/resources/templates/auth/invitation-landing.html"
      provides: "Landing page UI"
  key_links:
    - from: "src/main/java/de/goaldone/authservice/controller/InvitationController.java"
      to: "VerificationTokenService"
      via: "Token validation"
---

<objective>
Implement the user-facing invitation acceptance flow. This covers landing page logic for new vs. existing users and the password-setting mechanism for initial activation.

Purpose: To provide a smooth onboarding experience for users invited to organizations.
Output: Complete invitation acceptance flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@4-CONTEXT.md
@.planning/phases/04-invitation-reset-flows/RESEARCH.md
@.planning/phases/04-invitation-reset-flows/04-01-SUMMARY.md
@.planning/phases/04-invitation-reset-flows/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invitation Landing Page and Link Verification</name>
  <files>src/main/java/de/goaldone/authservice/controller/InvitationController.java, src/main/resources/templates/auth/invitation-landing.html, src/test/java/de/goaldone/authservice/controller/InvitationFlowTests.java</files>
  <behavior>
    - GET /invitation?token={token} validates the token via VerificationTokenService.
    - If user exists and is logged out: Show landing page with 'Log in to accept' message (D-09).
    - If user exists and is logged in: Show 'Accept Invitation' button.
    - If user does not exist: Redirect to password setting page.
    - Invalid or expired tokens show an error page.
  </behavior>
  <action>
    - Create `InvitationController`.
    - Implement landing logic using `VerificationTokenService` and `UserRepository`.
    - Create `invitation-landing.html` using the base layout.
    - Update `InvitationManagementService` to generate a `VerificationToken` when an invitation is created (using Task 1 from 04-01).
  </action>
  <verify>
    <automated>./mvnw test -Dtest=InvitationFlowTests#testLandingPageLogic</automated>
  </verify>
  <done>The invitation landing page correctly identifies user state and token validity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invitation Acceptance and New User Activation</name>
  <files>src/main/java/de/goaldone/authservice/controller/InvitationController.java, src/main/resources/templates/auth/invitation-set-password.html, src/test/java/de/goaldone/authservice/controller/InvitationFlowTests.java</files>
  <behavior>
    - Authenticated users clicking 'Accept' are added to the organization and redirected to success.
    - New users on /invitation/set-password can submit a password.
    - Successful password submission transitions UserStatus from INVITED to ACTIVE and marks email as verified (F4.3).
  </behavior>
  <action>
    - Implement POST `/invitation/accept` for authenticated users.
    - Implement GET/POST `/invitation/set-password` for new users.
    - Create `invitation-set-password.html`.
    - Logic for creating membership and updating user status in `InvitationManagementService` or `UserManagementService`.
  </action>
  <verify>
    <automated>./mvnw test -Dtest=InvitationFlowTests#testActivationFlow</automated>
  </verify>
  <done>Users can successfully join organizations and activate accounts via invitations.</done>
</task>

</tasks>

<verification>
- Invitation landing page handles all user states (logged-in, logged-out, new).
- New users can activate accounts by setting a password.
- Existing users can link accounts to new organizations.
</verification>

<success_criteria>
- InvitationFlowTests pass.
- VerificationToken is consumed upon successful acceptance.
- User status transitions correctly in database.
</success_criteria>

<output>
After completion, create `.planning/phases/04-invitation-reset-flows/04-03-SUMMARY.md`
</output>
