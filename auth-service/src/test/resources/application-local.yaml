spring:
  datasource:
    # Isolate H2 per test ApplicationContext to avoid Liquibase changelog table races.
    url: jdbc:h2:mem:auth_test_${random.uuid};MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
