name: goaldone-prod

networks:
  zitadel:
    external: true   # Traefik's existing network — services discovered here
  app-prod:
    name: app-prod   # Internal network for frontend↔backend communication

services:
  # PostgreSQL database for the application
  postgres-prod:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: goaldone
      POSTGRES_USER: goaldone
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-prod-data:/var/lib/postgresql/data
    networks:
      - app-prod
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U goaldone"]
      interval: 10s
      timeout: 5s
      retries: 5

  # PostgreSQL database for auth-service (separate from backend per D-02)
  postgres-auth-prod:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${AUTH_SERVICE_DB_NAME:-auth_service}
      POSTGRES_USER: ${AUTH_SERVICE_DB_USER:-auth_service}
      POSTGRES_PASSWORD: ${AUTH_SERVICE_DB_PASSWORD}
    volumes:
      - postgres-auth-prod-data:/var/lib/postgresql/data
    networks:
      - app-prod
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${AUTH_SERVICE_DB_USER:-auth_service}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Auth-service (not exposed publicly — accessed by backend for JWKS)
  auth-service-prod:
    image: ghcr.io/goaldone-dhbw/goaldone-auth-service:${AUTH_SERVICE_VERSION}
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-auth-prod:5432/${AUTH_SERVICE_DB_NAME:-auth_service}
      SPRING_DATASOURCE_USERNAME: ${AUTH_SERVICE_DB_USER:-auth_service}
      SPRING_DATASOURCE_PASSWORD: ${AUTH_SERVICE_DB_PASSWORD}
      AUTH_SERVICE_RSA_PRIVATE_KEY: ${AUTH_SERVICE_RSA_PRIVATE_KEY}
      AUTH_SERVICE_JWT_ISSUER_URI: ${AUTH_SERVICE_JWT_ISSUER_URI}
      FRONTEND_CLIENT_ID: ${FRONTEND_CLIENT_ID:-goaldone-frontend}
    networks:
      - app-prod
    depends_on:
      postgres-auth-prod:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9000/actuator/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # Spring Boot backend (not exposed publicly)
  backend-prod:
    image: ghcr.io/goaldone-dhbw/goaldone-backend:${BACKEND_VERSION}
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: prod
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-prod:5432/goaldone
      SPRING_DATASOURCE_USERNAME: goaldone
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      AUTH_SERVICE_JWT_ISSUER_URI: ${AUTH_SERVICE_JWT_ISSUER_URI}
    networks:
      - app-prod
    depends_on:
      postgres-prod:
        condition: service_healthy
      auth-service-prod:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/api/v1/actuator/health || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s

  # Frontend - Blue slot (blue-green deployment)
  frontend-blue:
    image: ghcr.io/goaldone-dhbw/goaldone-frontend:${FRONTEND_VERSION}
    restart: unless-stopped
    environment:
      OIDC_ISSUER_URI: ${AUTH_SERVICE_JWT_ISSUER_URI}
      OIDC_CLIENT_ID: ${FRONTEND_CLIENT_ID}
      API_BASE_PATH: /api/v1
      BACKEND_HOST: backend-prod
    networks:
      - zitadel
      - app-prod
    labels:
      - traefik.enable=${FRONTEND_BLUE_ACTIVE:-false}
      - traefik.docker.network=zitadel
      # HTTPS router
      - traefik.http.routers.goaldone-frontend-prod.rule=Host(`goaldone.de`)
      - traefik.http.routers.goaldone-frontend-prod.entrypoints=websecure
      - traefik.http.routers.goaldone-frontend-prod.tls=true
      - traefik.http.routers.goaldone-frontend-prod.tls.certresolver=le
      - traefik.http.services.goaldone-frontend-prod.loadbalancer.server.port=80
      # HTTP → HTTPS redirect
      - traefik.http.routers.goaldone-frontend-prod-http.rule=Host(`goaldone.de`)
      - traefik.http.routers.goaldone-frontend-prod-http.entrypoints=web
      - traefik.http.routers.goaldone-frontend-prod-http.middlewares=redirect-to-https-prod@docker
      - traefik.http.middlewares.redirect-to-https-prod.redirectscheme.scheme=https
      - traefik.http.middlewares.redirect-to-https-prod.redirectscheme.permanent=true
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost/ > /dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Frontend - Green slot (blue-green deployment)
  frontend-green:
    image: ghcr.io/goaldone-dhbw/goaldone-frontend:${FRONTEND_VERSION}
    restart: unless-stopped
    environment:
      OIDC_ISSUER_URI: ${AUTH_SERVICE_JWT_ISSUER_URI}
      OIDC_CLIENT_ID: ${FRONTEND_CLIENT_ID}
      API_BASE_PATH: /api/v1
      BACKEND_HOST: backend-prod
    networks:
      - zitadel
      - app-prod
    labels:
      - traefik.enable=${FRONTEND_GREEN_ACTIVE:-false}
      - traefik.docker.network=zitadel
      # HTTPS router
      - traefik.http.routers.goaldone-frontend-prod-green.rule=Host(`goaldone.de`)
      - traefik.http.routers.goaldone-frontend-prod-green.entrypoints=websecure
      - traefik.http.routers.goaldone-frontend-prod-green.tls=true
      - traefik.http.routers.goaldone-frontend-prod-green.tls.certresolver=le
      - traefik.http.services.goaldone-frontend-prod-green.loadbalancer.server.port=80
      # HTTP → HTTPS redirect
      - traefik.http.routers.goaldone-frontend-prod-green-http.rule=Host(`goaldone.de`)
      - traefik.http.routers.goaldone-frontend-prod-green-http.entrypoints=web
      - traefik.http.routers.goaldone-frontend-prod-green-http.middlewares=redirect-to-https-prod@docker
      - traefik.http.middlewares.redirect-to-https-prod.redirectscheme.scheme=https
      - traefik.http.middlewares.redirect-to-https-prod.redirectscheme.permanent=true
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost/ > /dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres-prod-data:
  postgres-auth-prod-data:
