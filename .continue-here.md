---
phase: Auth-Service OIDC Integration (v1.0)
task: OAuth2 Token Exchange Debugging
status: in_progress
last_updated: 2026-05-03T21:15:00+02:00
---

## Current State

**Problem**: Frontend stuck in redirect loop after successful login
- User logs in to auth-service ✅
- Gets authorization code ✅  
- POST `/oauth2/token` is sent ✅
- **BLOCKED**: Token response fails with "No resource with given identifier" — response is 3xx or malformed

**Last action**: 
- Fixed `/oauth2/authorize` endpoint to require authentication (`.authenticated()`) — now redirects to login correctly
- Added CSRF disable to DefaultSecurityConfig
- Improved CallbackPageComponent with URL cleanup and event listening

## Work Completed This Session

1. **Fixed serialization issue in CustomUserDetails**
   - Problem: NotSerializableException when storing User entity with non-serializable collections
   - Solution: Extracted superAdmin flag and memberships as lightweight MembershipInfo objects
   - Files: CustomUserDetails.java, TokenCustomizerConfig.java, InvitationController.java
   - Status: ✅ Compiles, no serialization errors on login

2. **Fixed CORS configuration**
   - Added CorsConfigurationSource bean to DefaultSecurityConfig
   - Applied `.cors()` to all security filter chains
   - Files: DefaultSecurityConfig.java, application.yaml
   - Status: ✅ CORS headers present in responses

3. **Fixed authorization flow redirect to login**
   - Changed `/oauth2/authorize` from `.permitAll()` to `.authenticated()`
   - Configured proper exceptionHandling with LoginUrlAuthenticationEntryPoint
   - Files: AuthorizationServerConfig.java
   - Status: ✅ Unauthenticated users redirect to `/login`

4. **Improved callback handling**
   - Created CallbackPageComponent to handle token exchange result
   - Added URL cleanup with `history.replaceState()`
   - Added OAuthService event listening
   - Files: callback-page.component.ts, callback-page.component.html
   - Status: ✅ Component structure solid, but token response problem remains

## Remaining Work

**CRITICAL BLOCKER**: POST `/oauth2/token` response is failing
- Need to determine exact HTTP status and response format
- Suspected: 302 Redirect instead of 200 with token in body
- Or: CSRF token mismatch
- Or: Session/cookie not transmitted with POST request

**After token exchange fixed**:
1. Frontend receives token successfully
2. Test full login flow: login → authorization → token exchange → redirect to `/app`
3. Verify backend API calls work with JWT token
4. Test logout flow

## Decisions Made

- **Session-based auth for login flow**: Used Spring Session JDBC to maintain authentication state during OAuth2 flow (better UX than requiring login twice)
- **Extracted serializable membership info**: Instead of storing full User entity in CustomUserDetails, extract only needed serializable data
- **PKCE enforcement**: Required `requireProofKey: true` for frontend client — good for security

## Blockers

| Blocker | Type | Status |
|---------|------|--------|
| POST `/oauth2/token` response format unknown | Technical | Active — need user to inspect Network tab |
| CSRF protection may be too strict | Technical | Partially fixed (added `.csrf(csrf -> csrf.disable())`) |
| Session/cookie transmission across ports (localhost:4200 → localhost:9000) | Technical | Needs verification |

## Human Actions Pending

1. **User must inspect Network tab**:
   - Click on POST `/oauth2/token` request
   - Check **HTTP Status Code** (200, 302, 400, etc.)
   - Check **Response tab** — what does server actually return?
   - Report back with findings

2. **Restart services after CSRF fix**:
   - Kill running auth-service
   - Rebuild and restart with new CSRF config
   - Test login flow again

## Uncommitted Changes

```
M .env-example
M .gitignore
M auth-service/src/main/java/de/goaldone/authservice/config/AuthorizationServerConfig.java
M auth-service/src/main/java/de/goaldone/authservice/config/DefaultSecurityConfig.java
M auth-service/src/main/java/de/goaldone/authservice/config/TokenCustomizerConfig.java
M auth-service/src/main/java/de/goaldone/authservice/controller/InvitationController.java
M auth-service/src/main/java/de/goaldone/authservice/security/CustomUserDetails.java
M auth-service/src/main/resources/application.yaml
M auth-service/var/auth-service/keys/jwk.json
M frontend/src/app/features/callback/callback-page.component.html
M frontend/src/app/features/callback/callback-page.component.ts
?? frontend/package-lock.json
```

## Background Processes

Auth-service running on port 9000 (may need restart after CSRF changes)

## Next Action

1. **User**: Check Network tab, report HTTP status of POST `/oauth2/token`
2. **If response is 302 Redirect**: Likely CSRF or session issue — check auth-service logs for why token endpoint redirects
3. **If response is 200 with valid token**: Frontend may need to handle response differently
4. **Restart and test**: After CSRF disable is applied, restart auth-service and test full login flow

## Context & Thinking

The OAuth2 flow is mostly working:
- Discovery endpoints ✅
- Authorization endpoint ✅
- Login page ✅
- Authorization code generation ✅
- Token exchange **partially** (request sent, response fails)

The issue is in the token response handling. The frontend is sending a correct POST request with all required PKCE parameters, but the server's response is either:
1. Not a valid OAuth2 token response (wrong format)
2. A redirect (302) instead of a token response (200)
3. A 400 error we haven't seen yet

Once we know the exact problem, the fix should be straightforward — likely just one configuration change in DefaultSecurityConfig or AuthorizationServerConfig.

The architecture is sound; it's a configuration detail blocking us.
