opencollection: 1.0.0

info:
  name: GoalDone API
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: oauth2
    flow: authorization_code
    authorizationUrl: "{{authorizationUrl}}"
    accessTokenUrl: "{{tokenUrl}}"
    refreshTokenUrl: "{{tokenUrl}}"
    callbackUrl: https://oauth.usebruno.com/callback
    credentials:
      clientId: "{{clientId}}"
      placement: body
    scope: openid profile email offline_access urn:zitadel:iam:user:resourceowner
    pkce: {}
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: true
      autoRefreshToken: true
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
    openapi:
      - sourceUrl: ../../../../api-spec/openapi.yaml
        groupBy: path
        lastSyncDate: 2026-04-17T08:07:34.939Z
        specHash: be2678aeadde70859136f1c5de129ea0
        autoCheck: true
        autoCheckInterval: 5
