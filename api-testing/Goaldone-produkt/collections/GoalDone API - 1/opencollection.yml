opencollection: 1.0.0

info:
  name: GoalDone API
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: oauth2
    flow: authorization_code
    authorizationUrl: "{{authorizationUrl}}"
    accessTokenUrl: "{{tokenUrl}}"
    refreshTokenUrl: "{{tokenUrl}}"
    callbackUrl: https://oauth.usebruno.com/callback
    credentials:
      clientId: "{{clientId}}"
      placement: body
    scope: openid profile email offline_access urn:zitadel:iam:user:resourceowner
    pkce: {}
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: true
      autoRefreshToken: true
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
    openapi:
      - sourceUrl: ../../../../api-spec/openapi.yaml
        groupBy: path
        lastSyncDate: 2026-04-22T19:37:47.280Z
        specHash: 4ee4ff1ec1a20745d6272bab03697094
        autoCheck: true
        autoCheckInterval: 5
