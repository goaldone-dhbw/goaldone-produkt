info:
  name: Liste aller Super-Admins
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/admins/super-admins"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
