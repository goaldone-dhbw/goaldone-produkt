info:
  name: Alle Unternehmen auflisten
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admins/organizations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
