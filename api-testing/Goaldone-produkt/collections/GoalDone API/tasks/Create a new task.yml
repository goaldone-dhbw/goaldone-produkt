info:
  name: Create a new task
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/tasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
